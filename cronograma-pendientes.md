# Cronograma — Puntos Neggo (backend)

Última actualización: 28 de agosto de 2026. Compilado desde `docs/roadmap-pendientes.md` — este documento es la vista "qué falta y cuándo", el otro es el registro detallado de lo ya hecho. Mismo patrón que `neggo-12/docs/cronograma-pendientes.md`.

Las fases están pensadas en **sesiones de trabajo**, no en fechas de calendario fijas — el ritmo real depende de disponibilidad de Jhey y de cuántas decisiones de negocio queden abiertas en el camino, no de un plazo artificial.

## Fase 0 — Fundación (esta sesión, 17 ago)

- Lectura completa de `sistema-puntos-unificado.md`.
- Verificación real de Supabase: proyecto `puntos-neggo` (`ckxoypzsmvxhqlvlwnib`) **ACTIVE_HEALTHY**, sin tablas ni migraciones todavía.
- Documentación base creada: este cronograma, `metodologia-trabajo.md`, `modelo-economico-v1.md`, `roadmap-pendientes.md`.
- **Estado:** hecho.

## Fase 1 — Ledger base — **hecho y verificado, 17 ago**

- `clientes_puntos` (constraint único `(tipo_documento, numero_documento)`), `puntos_movimientos` (ledger append-only; el vencimiento a 12 meses vive en `fecha_vencimiento` de cada fila `ganado`, mismo patrón que `puntos_movimientos` de `neggo-12` — se descartó una tabla `lotes_puntos` separada por ser una segunda implementación de algo que ya existe), `canjes`, `audit_log`.
- RLS habilitada en las 4 tablas, sin policies (deny-all) — probada en ambos sentidos con `SET LOCAL ROLE anon/authenticated` (0 filas, `permission denied` en las funciones). Solo `service_role` tiene acceso.
- `otorgar_puntos` (1X/2X calculado internamente, no confía en el caller; 3X/5X quedan afuera hasta que exista infraestructura de campañas) y `solicitar_canje` como `SECURITY DEFINER`, con idempotencia por `referencia_externa`.
- Verificado con datos reales end-to-end (detalle en `roadmap-pendientes.md`) — 2 bugs reales encontrados y corregidos en el camino (grants a `PUBLIC` en vez de `anon`/`authenticated`; ambigüedad de nombre de columna en dos funciones).
- **Hallazgo pendiente de decisión de Jhey (no bloquea, pero hay que resolverlo antes de Fase 3):** `neggo-12` ya tiene su propio sistema de puntos (`puntos_movimientos`, tasa negociada por comercio) — ¿se migra/desactiva a favor de este, o coexisten un tiempo?

## Fase 2 — Código de verificación de canje — **hecho, incluido en Fase 1**

- Se revisó la definición real de `comercio_contactos.codigo_verificacion` en `neggo-12` antes de copiar el patrón — mismo hash+salt, salt propio del proyecto.
- Trigger + función `generar_codigo_verificacion_canje` sobre `canjes`, ya aplicado y probado (genera formato `"XXX XXX"`).

## Fase 3 — Integración servidor-a-servidor — **desplegado, 17 ago; falta 1 paso manual de Jhey**

- Edge Functions `otorgar-puntos` y `solicitar-canje` desplegadas en `puntos-neggo` (`verify_jwt=false`, autenticación propia por header `x-internal-secret`). No se investigó la integración `neggo-12` ↔ `ads-ai-platform` — Jhey aclaró que un proyecto aparte va a conectar las APIs de todos los productos, así que ese patrón puntual ya no es la referencia a copiar. El contrato de estas dos funciones (URL + header + JSON) sirve igual para quien termine llamándolas.
- **Nota sobre el sistema de puntos viejo de `neggo-12`:** Jhey confirmó que era la versión inicial y que va a desaparecer — solo queda el visual. No se tocó ni se va a tocar ese código desde `puntos-neggo`.
- **Pendiente de Jhey (no técnico, un paso de Dashboard):** cargar el secreto `INTERNAL_SECRET` en Supabase — sin eso las funciones responden `server_misconfigured` a propósito (fail-closed). Instrucción exacta en el mensaje de cierre de esta sesión.
- **Verificado en producción, 17 ago:** Jhey corrió el curl real y `otorgar_puntos` devolvió `puntos_otorgados: 2000, multiplicador: 2` — correcto para primera compra de $800.000. Dato de prueba borrado después. Fase 3 cerrada.

## Fase 4 — Panel admin mínimo, comercios solo-canje — **hecho y verificado en vivo, 17 ago**

- `comercios_solo_canje` (nombre, ciudad, contacto, datos de pago) con RLS restringida a `admins` (allowlist por `auth.uid()`, no a cualquier `authenticated`).
- `marcar_canje_pagado` como `SECURITY DEFINER`, transición explícita `pendiente_pago → pagado` (rechaza cualquier otro estado de origen), con `_log_audit`.
- Panel: página HTML sola (`admin/index.html`, sin framework — herramienta de un botón para un usuario, no ameritaba Vite/Cloudflare), login con Supabase Auth, lista de canjes pendientes con el código de verificación visible, botón "Marcar entregado", alta de comercios.
- 2 bugs de grants/RLS encontrados y corregidos en el camino (`anon` podía ejecutar `marcar_canje_pagado`; las policies de `admins` bloqueaban a un admin real por RLS-sobre-RLS — ver roadmap para el detalle).
- **Verificado por Jhey en el navegador real:** alta de comercio, y flujo completo de canje (otorgar puntos → solicitar canje → ver pendiente en el panel con código de verificación → marcar entregado) — confirmado también a nivel de base (`estado='pagado'`, `pagado_por` = su usuario real, `audit_log` con el evento). Datos de prueba eliminados después.
- **Pendiente, no bloqueante:** hosting persistente del panel (hoy corre con `python3 -m http.server` local) — decisión aparte cuando haga falta.
- **Pendiente de Jhey:** crear su usuario de Supabase Auth (instrucción exacta en el mensaje de cierre) — sin eso no hay forma de probar el flujo completo de admin autorizado, solo se verificaron los caminos de rechazo (no-admin, anon).

## Fase 5 — Pasivo financiero visible — **hecho y verificado, 17 ago**

- Función `obtener_pasivo_puntos()` (no `SECURITY DEFINER` a propósito — la RLS existente de `admins` la protege sola). Devuelve puntos en circulación, valor de referencia en COP, clientes con saldo.
- Panel (`admin/index.html`) con sección visible arriba de "Canjes pendientes".
- Verificado con datos de prueba: admin ve el total real, no-admin ve ceros. Confirmado también visualmente por Jhey en el navegador real.

## Fase 6 — Mecánicas avanzadas del modelo V1 (estimado: 2–3 sesiones, varias sub-tareas independientes)

- **Sub-tarea 1, vencimiento por lote (barrido) — hecho y verificado, 17 ago.** `ejecutar_barrido_vencimientos()` recalcula consumo FIFO sobre el ledger y vence lo que corresponda de cada lote pasado su `fecha_vencimiento`; programado por `pg_cron` diario (01:00 Bogotá). Idempotente, probado con datos reales (2 lotes, consumo parcial, vencimiento parcial correcto, segunda corrida sin duplicar). Detalle en `roadmap-pendientes.md`. El pasivo financiero (Fase 5) ahora es exacto con hasta 24h de rezago, ya no es una estimación.
- Presupuesto máximo por campaña con corte automático — **pendiente, sin decisión de Jhey sobre el mecanismo exacto de corte.**
- Transferencia punto a punto con límites antifraude y preservación de vencimiento del lote de origen — **pendiente, sin decisión de Jhey sobre límites diario/mensual.** (El ledger y el cálculo FIFO ya están listos para sumar `transferido_entrada`/`transferido_salida` cuando se construya.)
- **Sub-tarea 2, reversos y fraude — hecho y verificado, 17 ago.** `revertir_puntos_otorgados` (reversión de compra cancelada/devuelta, idempotente, recupera como máximo el saldo disponible y deja constancia de lo no recuperado), `congelar_cliente`/`descongelar_cliente` (bloquea todo), `bloquear_redencion_cliente`/`desbloquear_redencion_cliente` (bloquea solo canje). **En el camino se encontró y corrigió un hallazgo de seguridad real:** las 5 funciones quedaron ejecutables por `anon` (sin login) hasta que se detectó con `get_advisors` y se corrigió — detalle completo en `roadmap-pendientes.md`.
- **Sub-tarea 3, notificación de vencimiento próximo — hecho y verificado, 17 ago.** `puntos_por_vencer_cliente` + Edge Function `puntos-por-vencer`, confirmado con curl real. Ventana configurable (default 15 días, el mismo número de ejemplo de la spec -- la ventana final exacta sigue siendo decisión de Jhey). De paso se corrigió un gap real: `revertir_puntos_otorgados` (sub-tarea 2) nunca tuvo Edge Function propia -- ya desplegada (`revertir-puntos`) y confirmada con curl real (200 puntos otorgados y revertidos de punta a punta).
- **Sub-tarea 4, transferencia punto a punto — hecho y verificado, 17 ago.** `transferir_puntos` + Edge Function `transferir-puntos`. Preserva el vencimiento del lote de origen (puede generar varias filas en el destinatario si la transferencia toca más de un lote). **Límites confirmados por Jhey, 18 ago:** 2.000 puntos por transferencia, 3.000/día, 10.000/mes, 3 transferencias/día -- fáciles de cambiar en una migración futura si hace falta. Probado con transferencia multi-lote real, idempotencia, los 8 casos de rechazo, y regresión completa del barrido de vencimientos/notificación con puntos recibidos por transferencia. Detalle en `roadmap-pendientes.md`.

## Fase 6 completa

Las 4 sub-tareas (vencimiento por lote, reversos y fraude, notificación de vencimiento, transferencia punto a punto) están hechas y verificadas con datos reales. Los límites de transferencia quedaron confirmados por Jhey el 18 de agosto. Sin pendientes técnicos en esta fase.

## Fase 7 — Retiro en efectivo y pagos a comercios (arrancó parcialmente, 19 ago)

Jhey empezó conversación con una pasarela de pagos. Sigue bloqueado el envío real de plata (proveedor definitivo + consulta jurídica/SARLAFT), pero ya se puede avanzar la parte técnica que no depende de eso.

**Decisión de alcance importante (19 ago):** el comprobante de venta (monto pagado, producto/servicio) NO lo genera puntos-neggo — es responsabilidad del admin de Talleres/CarroTalleres, que tiene el contexto de la cita/servicio. Puntos-neggo ya soporta el enlace vía `referenciaExterna` en `otorgar_puntos` (existe desde Fase 1) más las columnas `valor_compra`/`multiplicador` ya persistidas en el ledger — no hizo falta ninguna migración para esto. Se le entregó al arquitecto de CarroTalleres el contrato exacto del endpoint `otorgar-puntos` para que genere el comprobante de su lado y lo enlace.

**Sub-tarea, cuenta por pagar por comercio — hecho y verificado, 19 ago.** `obtener_pasivo_por_comercio()` agrupa `canjes` en estado `pendiente_pago` por comercio (mismo patrón que el pasivo de clientes de Fase 5). Verificado con datos de prueba reales: admin ve los montos correctos por comercio, no-admin ve cero filas, `anon` no puede ni ejecutar la función (`permission denied`). Agregado al panel admin como nueva sección "Cuenta por pagar por comercio", y `marcar_canje_pagado` ahora refresca esa tabla también. Datos de prueba eliminados después.

**Sub-tarea, portal de comercios (login propio) — hecho y verificado, 20 ago.** Nueva app `comercio/index.html`, login con Supabase Auth independiente del admin. Diseño: `comercios_solo_canje.user_id` vincula el comercio a su cuenta (el admin lo crea manualmente en Supabase Dashboard y lo vincula desde el panel admin, no hay autoregistro). Nuevas RLS: el comercio ve solo su propia fila de `comercios_solo_canje` y solo sus propios `canjes` — esto además hace que `obtener_pasivo_por_comercio()` funcione automáticamente para el comercio sin duplicar lógica, la RLS ya filtra antes de agrupar.

Nueva función `confirmar_canje_comercio(canje_id, codigo)`: el comercio confirma el código que le da el cliente en persona. Decisión de diseño importante: el código de verificación **nunca se expone al comercio por consulta directa** (el portal no lo selecciona) — solo se puede confirmar escribiéndolo, y el servidor lo compara. Esto NO cambia `canjes.estado` (que sigue significando "Neggo ya pagó") — se guarda en una columna nueva `confirmado_comercio_at`, evitando romper el pasivo por comercio o cualquier filtro existente por estado. El pago real lo sigue marcando el admin (o, más adelante, la automatización con la pasarela).

**Bug real encontrado y corregido en el camino:** ambigüedad de nombre de columna en `confirmar_canje_comercio` (el parámetro de salida y la columna de la tabla se llamaban igual) — mismo patrón de bug que ya había mordido el proyecto en Fase 1. Corregido con una migración nueva (`fix_ambiguedad_confirmar_canje_comercio`), nunca se tocó la migración original.

**Verificado con datos y usuarios de prueba reales (limpiados después):** un comercio ve únicamente su propia fila y sus propios canjes, nunca los de otro comercio (probado con 2 comercios simultáneos). Código incorrecto rechazado. Un comercio ajeno con el código correcto en la mano igual no puede confirmar un canje que no es suyo (`No autorizado`). Confirmación exitosa deja `confirmado_comercio_at` pero `estado` sigue en `pendiente_pago`. Doble confirmación rechazada. `anon` no puede ejecutar ninguna función nueva (`permission denied`). `get_advisors(security)` limpio -- solo los WARN ya esperados de funciones `SECURITY DEFINER` intencionalmente callable por `authenticated`.

**Sub-tarea, llave pública del cliente (`llave_cliente`) — hecho y verificado, 27 ago.** Jhey definió que cada cliente necesita una "llave" memorable para identificarse en la futura página de Puntos Neggo: primer nombre + últimos 2 dígitos de la cédula (ej. `jheison68`). Se agregó `clientes_puntos.llave_cliente` (columna nueva, `unique`), generada automáticamente por trigger cuando el cliente tiene `nombre` guardado, con manejo de colisión (si `jheison68` ya existe, crece el sufijo a 3+ dígitos hasta encontrar uno libre). `otorgar_puntos` se extendió con un `p_nombre` opcional (parámetro 8, con default `null` — no rompe ningún caller existente) para poder llenar `nombre` la primera vez que se conoce (gana el primer nombre conocido, una compra posterior con otro nombre no lo pisa). El Edge Function `otorgar-puntos` ya se actualizó para mandarlo.

**Error real encontrado y corregido en el camino, más serio que el de nombres de columna:** el primer intento guardó la llave en la columna `codigo_publico`, que YA EXISTÍA desde Fase 1 con un propósito completamente distinto (un código opaco derivado del id interno, `'PT-' + primeros 6 del id`, para que un comercio verifique la identidad de un cliente sin exponer el documento — ver `sistema-puntos-unificado.md` sección 3, "Los dos códigos"). No se revisó qué ya existía antes de escribir la migración. Se detectó de inmediato al verificar con datos reales (el trigger nuevo escribía un valor que un trigger viejo, alfabéticamente posterior, pisaba en cada INSERT). Nunca llegó a producción real (0 clientes reales todavía). Corregido con una migración nueva (`llave_cliente_corrige_colision_codigo_publico`) que deshace el trigger equivocado y crea la columna `llave_cliente` separada — `codigo_publico` sigue funcionando exactamente igual que desde Fase 1, sin tocar.

**Segundo hallazgo en el camino:** `create or replace function` con una lista de parámetros distinta crea un *overload* nuevo en vez de reemplazar la función — quedaron dos versiones de `otorgar_puntos` (7 y 8 parámetros) coexistiendo. La vieja nunca tuvo permiso para `anon`/`authenticated` (sin hueco de seguridad), pero se eliminó para no mantener dos copias de la misma lógica (migración `elimina_overload_viejo_otorgar_puntos`) — una llamada con los 7 argumentos de siempre sigue funcionando igual, gracias al default de `p_nombre`.

**Tercer hallazgo, el más importante de seguridad:** `get_advisors` + consulta directa a permisos confirmó que este proyecto otorga `EXECUTE` a `anon`/`authenticated` por defecto al crear cualquier función nueva (el mismo comportamiento de proyecto ya conocido de fases anteriores) — se me olvidó revocarlo en 3 funciones internas nuevas (con prefijo `_`, nunca deberían llamarse directo): `_normalizar_para_codigo`, `_generar_llave_cliente`, `_trigger_generar_llave_cliente`. Corregido en una migración aparte (`revoca_execute_helpers_llave_cliente`). Re-verificado con `get_advisors`: limpio, sin hallazgos nuevos.

**Verificado con datos de prueba reales (limpiados después):** `jheison68` se genera exactamente como el ejemplo de Jhey; 2 clientes más con el mismo nombre y mismos últimos 2 dígitos generan `jheison868`/`jheison768` (colisión resuelta creciendo el sufijo); nombre con acentos y dos palabras (`María José`) genera `maria99` (solo primer nombre, normalizado); llamada estilo Edge Function actual (sin `p_nombre`) sigue funcionando igual que hoy; llamada con `p_nombre` llena `nombre` y genera la llave; una segunda compra con un nombre distinto no pisa el nombre ya guardado (`Carlos` se queda, no lo reemplaza `Carlos Alberto Otro`).

**Decisión de Jhey (27 ago) que cambia el plan anterior:** no esperar el PIN temporal — construir ya toda la página de Puntos Neggo, identificando al cliente **solo por número de documento** mientras tanto. El PIN sigue siendo el plan de verificación real, pero se pospone; esto se documenta como deuda de seguridad explícita, no un olvido.

**Sub-tarea, portal del cliente (`cliente/index.html`) — hecho y verificado, 27 ago.** Primera página de este proyecto pensada para que el cliente final la use directamente (hasta ahora todo era admin/comercio/servidor-a-servidor). Pantallas: identificarse por documento, ver saldo + historial + puntos por vencer en 30 días, transferir puntos a otro cliente por su llave (`jheison68`), solicitar canje en un comercio disponible y ver el código de verificación para dárselo en persona.

Backend nuevo: 5 funciones `cliente_portal_*` (`estado`, `buscar_destinatario`, `transferir`, `comercios_disponibles`, `solicitar_canje`), todas `SECURITY DEFINER`, otorgadas a `anon` — la **primera vez que este proyecto expone funciones a un navegador anónimo**, rompiendo a propósito la regla histórica "nunca se llama desde el navegador de un cliente" (Fase 3/7). Diseño elegido para no arriesgar la lógica ya probada: estas funciones son wrappers nuevos que llaman por dentro a `transferir_puntos`/`solicitar_canje` ya existentes (que siguen siendo `service_role`-only, sin tocar) — mismos límites antifraude, misma trazabilidad, ningún atajo.

**Riesgo de seguridad real y consciente, no oculto:** con solo el número de documento como identidad, cualquiera que lo conozca puede ver el saldo de otra persona y mover sus puntos (transferir, canjear). Es una decisión de negocio de Jhey para poder avanzar la interfaz ya, con el PIN temporal como reemplazo planeado antes de anunciar la página públicamente. La página ya trae un aviso visible al cliente sobre esto ("por ahora te identificamos solo con tu documento").

**Bug real encontrado y corregido en el camino:** `cliente_portal_estado` asumió que `puntos_por_vencer_cliente` devolvía una columna `puntos_por_vencer` sin revisar la función real — el nombre correcto es `puntos_restantes`. Se detectó de inmediato al probar como `anon` con datos reales (error `column does not exist`), corregido en una migración aparte (`fix_columna_puntos_por_vencer_portal`).

**Verificado con datos y llamadas reales (como rol `anon`, limpiados después):** `cliente_portal_estado` devuelve saldo/nombre/llave/historial correctos; documento no registrado da un error claro; `cliente_portal_buscar_destinatario` encuentra al destinatario por llave y devuelve `[]` (no error) si la llave no existe; una transferencia real entre 2 clientes de prueba movió el saldo correctamente en ambos lados; `anon` sigue sin poder llamar `transferir_puntos` directamente (`permission denied`) — el wrapper es la única puerta; `cliente_portal_solicitar_canje` genera el canje con su código de verificación y descuenta el saldo; comercio inexistente y comercio no encontrado dan error claro. `get_advisors(security)` solo marca los 5 `cliente_portal_*` como ejecutables por `anon` — exactamente lo esperado, ninguna fuga nueva.

**Verificado también el frontend (`cliente/index.html`)** con un navegador real (Playwright) contra respuestas idénticas en forma a las reales ya probadas por SQL (la red de este entorno no permite probar contra Supabase en vivo desde aquí, así que se interceptó solo la capa de red, no la lógica): documento inexistente muestra el error correcto sin entrar; login correcto muestra saldo/llave/historial/aviso de puntos por vencer; la sesión persiste al recargar la página y se borra correctamente al salir; buscar un destinatario por llave muestra su nombre antes de confirmar la transferencia, y una llave inexistente no muestra nada; la transferencia actualiza el saldo en pantalla; el canje valida el mínimo de 200 puntos del lado del cliente y muestra el código de verificación al final. Cero errores de consola en todo el recorrido.

**Sub-tarea, home / página de aterrizaje (`index.html`) — hecho y verificado, 27 ago.** Página pública en la raíz del repo, pensada para dos públicos (decisión de Jhey): clientes finales y comercios nuevos. Secciones: hero con CTA a "Ver mis puntos" (`/cliente/`), cómo funciona en 3 pasos, beneficios, y una sección para comercios con propuesta de valor + formulario de afiliación, más un CTA final y footer con enlace al portal de comercios (`/comercio/`) para los ya afiliados.

Backend nuevo: tabla `solicitudes_comercio` para capturar el formulario — `anon` solo puede **insertar**, nunca leer (ni sus propios datos después de enviarlos); solo un admin puede ver y marcar atendida una solicitud, mismo patrón allowlist que el resto del panel. Se agregó una sección nueva "Solicitudes de comercios" en `admin/index.html` para que Jhey vea y gestione los leads que caigan ahí — sin esto el formulario habría quedado sin ningún lugar donde revisarlo.

**Verificado con datos e identidades reales (limpiados después):** `anon` puede insertar una solicitud pero no puede leer ninguna (`permission denied`); un usuario autenticado sin ser admin tampoco ve nada; el admin real ve la solicitud y puede marcarla `atendido = true`. `get_advisors(security)` sin hallazgos nuevos. El formulario y el resto de la página se probaron con un navegador real (Playwright, red interceptada de la misma forma que en el portal del cliente): validación de campo obligatorio, envío exitoso con el payload correcto, mensaje de confirmación, cero errores de consola.

Pendiente (no bloqueante, sub-fases siguientes): el PIN temporal de verificación (reemplazar la identificación por solo-documento), solicitud de pago anticipado, y el adaptador de pago (`PaymentProvider`) con disparo manual y programado.

El envío real de plata no se activa hasta resolver, en este orden:
1. Selección del proveedor financiero regulado (Wompi como primera validación de MVP, SEDPE/BaaS como objetivo, Bold como alternativa) — **gestión de Jhey, no técnica**.
2. Consulta jurídica/regulatoria formal (Ley 1735, régimen SEDPE, Superfinanciera) — **gestión de Jhey**.
3. Diseño del saldo prefondeado por comercio (no emitir puntos "a crédito").
4. Resolver o al menos decidir conscientemente el punto ciego de verificación de identidad de clientes en `neggo-12` (ya documentado en la skill `puntos-neggo-engineering`).

El ledger, la interfaz `PaymentProvider` (adapter, sin amarrar a proveedor) y todo lo de Fases 1–6 se construyen sin esperar esto — el retiro real es la única pieza que depende 100% de gestión externa.

**Sub-tarea, investigación de competencia (Puntos Colombia) — hecho, 27 ago.** A pedido de Jhey, se revisó el sitio público de Puntos Colombia (`puntoscolombia.com/personas/inicio`) únicamente como referencia de diseño/UX — explícitamente **no** para copiar nada. Se navegó con el navegador Chrome conectado de Jhey (no hay red directa a sitios externos desde este entorno) y se capturaron pantallas propias del sitio (hero, menú de categorías de canje, grid de marcas aliadas, sección "cómo funciona" en 3 pasos, footer con sello regulatorio) para usarlas como material de estudio.

Se armó un documento (`Radiografía Puntos Colombia`, publicado como Artifact: https://claude.ai/code/artifact/26956d26-8208-4b31-9e87-050989eb3819) que primero da contexto real de escala (Puntos Colombia es una alianza Bancolombia + Grupo Éxito, 300+ marcas aliadas — no es una comparación justa de tamaño) y después, patrón por patrón, dice explícitamente qué es adoptable ya, qué es adoptable más adelante (con qué condición: ej. "cuando tengamos 3+ categorías de canje" o "con 15–20 comercios activos"), y qué no aplica todavía — comparando cada patrón contra capturas reales de nuestras propias páginas ya construidas (`index.html`, `cliente/index.html`). Cierra con una tabla de recomendaciones concretas y accionables, y un recordatorio explícito de que ningún activo de Puntos Colombia fue reutilizado.

**Sub-tarea, visual para conseguir más comercios aliados — hecho y verificado, 27 ago.** A partir de la investigación de competencia (arriba), Jhey confirmó el objetivo: llegar a tener muchas marcas aliadas al programa. Se dio prioridad a tres piezas visuales que apuntan directamente a eso, sin backend nuevo (todo se apoyó en tablas/funciones que ya existían y ya estaban verificadas).

1. **Nueva página `aliados/index.html`** — la página dedicada para reclutar comercios (antes solo existía una sección chica dentro del home). Tiene su propio hero orientado al dueño de negocio, 4 razones para sumarse, 4 pasos de "cómo funciona para tu negocio", una sección "Primeros aliados" honesta (dice explícitamente que apenas se está construyendo la red), preguntas frecuentes (costo, cómo pagan, qué pasa si alguien miente sobre tener puntos, si hay que instalar algo), y el formulario completo de afiliación (el mismo que antes vivía en el home, movido para acá).
2. **`index.html` (home) simplificado** — la sección "Comercios" ya no repite el formulario completo: ahora es un resumen corto + una tarjeta con la llamada a la acción hacia `/aliados/#afiliar`. Se agregó una sección nueva "Primeros aliados" (con los mismos comercios reales, en formato compacto) entre "Beneficios" y "Comercios", para que un cliente vea que ya hay negocios reales antes de llegar a la parte de invitar a nuevos.
3. **Rediseño completo de `comercio/index.html`** — el portal de comercios ya afiliados se armó el 20 de agosto, antes del trabajo visual de esta sesión, y se había quedado con el estilo funcional original (el mismo de `admin/index.html`). Se rediseñó con el mismo sistema visual de `index.html`/`cliente/index.html` (tokens de color, tipografía, tarjetas) sin tocar ni un solo nombre de función ni de tabla — la lógica (login, `obtener_pasivo_por_comercio`, `confirmar_canje_comercio`, lectura de `canjes`) es exactamente la misma, solo cambió cómo se ve.

**Dato real usado para diseñar, no inventado:** hoy solo hay 1 comercio activo en `comercios_solo_canje` (`talleres`, Medellín) — se consultó directo en Supabase antes de construir. Por eso la sección "Primeros aliados" está diseñada para verse bien con pocos (o cero) comercios, no como un grid que necesita docenas para no verse vacío — el mismo tipo de decisión que recomendó el dossier de Puntos Colombia.

**De dónde sale la lista de "Primeros aliados":** se reutilizó `cliente_portal_comercios_disponibles()` (ya existente, ya otorgada a `anon` desde la sub-tarea del portal del cliente) — devuelve `nombre` y `ciudad` de los comercios `activo = true`. No hizo falta ninguna función ni permiso nuevo. El nombre/ciudad se les da formato de título en el navegador (`talleres` → `Talleres`) sin tocar los datos reales en la base.

**Verificado con un navegador real (Playwright, red interceptada de la misma forma que en el portal del cliente, mismas respuestas ya probadas por SQL en sub-tareas anteriores):** el home carga "Primeros aliados" con los comercios reales y las llamadas a la acción apuntan a `/aliados/`; en `aliados/index.html` el listado de aliados carga igual, el acordeón de preguntas abre/cierra, la validación del formulario y el envío exitoso funcionan igual que en la versión anterior del formulario; en `comercio/index.html` un login incorrecto muestra error, uno correcto entra y muestra el negocio/pasivo/canjes correctos, un código de confirmación incorrecto muestra error y uno correcto confirma el canje y refresca la lista, y salir vuelve a la pantalla de acceso. Cero errores de consola en los tres.

**Sub-tarea, tema claro (fondo blanco), barra de utilidad, banner y footer legal — hecho y verificado, 27 ago.** Jhey pidió, siguiendo el estudio de Puntos Colombia: cambiar el fondo oscuro por uno claro, agregar un banner similar al de Puntos Colombia, y organizar el pie de página con la parte legal (referenciando el reglamento público de Puntos Colombia como inspiración, no para copiarlo). Antes de tocar código se confirmaron 3 decisiones con Jhey vía preguntas: alcance del fondo claro = **todo el sitio** (las 5 páginas); tipo de banner = **ambos** (barra de utilidad tipo "Personas/Empresas" + banner promocional en el hero); contenido legal del footer = **estructura y enlaces con "Próximamente"**, sin inventar texto legal real (la consulta jurídica formal sigue pendiente de Jhey, ya documentada como bloqueo).

**Tema claro en las 5 páginas** (`index.html`, `aliados/index.html`, `cliente/index.html`, `comercio/index.html`, `admin/index.html`): mismo sistema de diseño de antes, mismos nombres de variable, solo se re-calibró la paleta para fondo claro. El acento (antes `#6d8dff`/`#9a6dff`, pensado para texto sobre fondo oscuro) se oscureció a `#4a5cd6`/`#7748cf` para mantener buen contraste como texto sobre blanco (verificado con la fórmula de contraste WCAG: ~5.1–5.8:1, pasa AA). Mismo criterio para verde/naranja/rojo semánticos. Ningún nombre de función, tabla o lógica de negocio se tocó — es 100% visual.

**Barra de utilidad ("Clientes / Comercios")**, arriba de todo, en las 5 páginas menos `admin` (panel interno de Jhey, no le aplica la segmentación de audiencia pública). Resuelve la navegación entre el lado cliente y el lado comercio de una forma más visible que antes.

**Banner promocional**, solo en el home: destaca el comercio activo real más reciente (ej. "ya puedes canjear tus puntos en Cafe Aroma, Bogotá"), leyendo del mismo `cliente_portal_comercios_disponibles()` ya usado para "Primeros aliados" — **nunca una oferta inventada**; si no hay comercios activos, el banner queda oculto en vez de mostrar un placeholder falso.

**Footer legal**: nueva página `legal/index.html` con tres secciones ancladas (`#reglamento`, `#terminos`, `#privacidad`), cada una marcada explícitamente "En preparación" — dice qué va a cubrir cada documento (ya con contenido real: vencimiento a 12 meses, límites de transferencia, etc.) sin redactar el texto legal vinculante, que queda pendiente de la consulta jurídica de Jhey. El footer de `index.html` y `aliados/index.html` se reorganizó en columnas (Producto / Legal) enlazando ahí; `cliente/index.html` y `comercio/index.html` (interfaces más compactas) llevan un enlace "Legal" simple en vez de columnas completas.

**Verificado con un navegador real (Playwright, red interceptada de la misma forma que en sub-tareas anteriores) en las 6 páginas:** fondo claro aplicado y confirmado por `getComputedStyle` (`rgb(245, 246, 251)`); banner promocional se arma con datos reales mockeados y queda oculto correctamente cuando no hay comercios; login y flujos de `cliente/`, `comercio/` y `admin/index.html` siguen funcionando exactamente igual que antes (mismo saldo, mismos canjes, mismo pasivo); los anclajes del footer legal (`/legal/#reglamento`, etc.) resuelven a la sección correcta. Cero errores de consola en las 6 páginas.

**Sub-tarea, rediseño de paleta de color y carrusel del home — hecho y verificado, 27 ago.** Jhey vio el resultado del tema claro anterior y dio feedback directo: los colores (`#4a5cd6`/`#7748cf`, azul-violeta) se parecían demasiado a los de Puntos Colombia y pidió no parecerse en nada — además pidió que el banner del home fuera un carrusel de verdad, en movimiento, como el de `puntoscolombia.com/personas/inicio`, y no la franja estática de la sub-tarea anterior. Pidió explícitamente meter rosado, azul y amarillo, revisando bien que combinen.

**Paleta nueva:** se reemplazó el acento azul-violeta por un azul más directo (`#2953d9`, contraste 6.30:1 sobre blanco) + un rosa vibrante (`#e23f84`) como `--accent-2`. El rosa solo se usa en gradientes/decorativos (nunca como texto plano sobre blanco: su contraste solo es 3.98:1, insuficiente para AA) — se auditó el CSS de las 6 páginas para confirmar que `--accent-2` nunca se usaba como texto suelto antes del cambio, así que es seguro. Se mantuvo `--orange: #8a5a12` sin tocar para los estados semánticos ("en preparación", "sin confirmar", "canje pendiente" — ya validado, no es identidad de marca). Para el amarillo/dorado que pidió Jhey se agregó un token nuevo, puramente decorativo, `--gold: #c97f0e` (usado solo en fondos y acentos del carrusel del home, nunca como texto plano — su contraste, 3.2:1, tampoco alcanza AA). Reemplazo mecánico del hex/rgb viejo confirmado por grep en las 6 archivos (38 ocurrencias reemplazadas, cero remanentes).

**Carrusel del home (`index.html`):** reemplaza la franja estática anterior. Tres diapositivas con fondos degradados distintos para que los 3 colores convivan sin competir entre sí (azul+rosa / rosa+dorado / dorado+azul, cada una con su propio acento de texto): (1) el comercio aliado real más reciente (mismo dato honesto de antes, vía `cliente_portal_comercios_disponibles()` — si no hay comercios activos, la diapositiva lo dice explícitamente en vez de ocultarse), (2) destaca las transferencias entre clientes, (3) invita a comercios a afiliarse, enlazando a `/aliados/#afiliar`. Avanza solo cada 6 segundos, con puntos de navegación manual, se pausa al pasar el mouse o el foco de teclado por encima, y respeta `prefers-reduced-motion` (usuarios con esa preferencia no ven auto-avance, pero conservan la navegación manual por puntos).

**Verificado con un navegador real (Playwright) en las 6 páginas:** cero errores de consola; el carrusel avanza solo cada ~6s, se detiene al pasar el mouse y al enfocar con teclado, los 3 puntos de navegación funcionan al hacer clic; con `prefers-reduced-motion: reduce` emulado, el auto-avance queda desactivado pero la navegación manual sigue funcionando; se confirmó por `getComputedStyle` que `--accent`/`--accent-2`/`--gold` tienen los valores nuevos en las 5 páginas con variables CSS, y que el botón de `admin/index.html` (hex fijo, sin variables) también quedó en el azul nuevo; búsqueda de los hex viejos (`4a5cd6`, `7748cf`) en el DOM renderizado confirma cero remanentes.

**Sub-tarea, rediseño visual del login del cliente (fase 1 de 2: visual) — hecho y verificado, 27 ago.** Jhey pidió organizar el login del portal cliente porque hoy no deja ningún dato expuesto y va a moverse a sesiones reales — pero pidió explícitamente hacerlo en dos fases: primero lo visual (futurista, acorde a la identidad azul/rosa/dorado ya construida), después la seguridad de fondo. Se le preguntó qué pantalla priorizar primero y confirmó **solo `cliente/index.html`** (el portal por documento — es el que más expone: hoy cualquiera con el número de documento de otra persona ve su saldo). `comercio/` y `admin/` (ya usan Supabase Auth con email/contraseña) quedan fuera de esta sub-tarea.

**Esta sub-tarea es 100% visual — cero cambios de seguridad o de lógica de autenticación.** Se mantiene exactamente la misma llamada a `cliente_portal_estado(tipo_documento, numero_documento)` y el mismo almacenamiento de sesión en `localStorage` de antes; la nota honesta ("por ahora te identificamos solo con tu documento...") se mantiene con el mismo texto, solo con mejor presentación. La fase de seguridad real (sesiones, código temporal / PIN) queda para la siguiente sub-tarea, ya documentada como pendiente desde la sub-tarea del portal del cliente.

**Cambios visuales en `cliente/index.html`:** fondo "aurora" — tres manchas de color (azul/rosa/dorado) desenfocadas que flotan despacio detrás de la tarjeta de login, contenidas solo dentro de esa pantalla (no se extendió al resto del sitio); tarjeta con efecto vidrio (`backdrop-filter: blur`) y borde con degradado de los 3 colores de marca; ícono de identificación en la tarjeta; selector de tipo de documento (CC/CE/TI/PA) que antes era un `<select>` plano, ahora es un control segmentado tipo pastillas (mismos 4 valores, mismo comportamiento, solo visual — implica un cambio mínimo de JS: una variable `tipoDocumentoActivo` reemplaza `$("login-tipo").value`); campo de número de documento con ícono; botón con efecto de brillo al pasar el mouse; la nota de seguridad se rediseñó como una tarjetita con ícono en vez de un párrafo gris plano, mismo texto. Todo con `--gold`/`--gold-soft` agregados como tokens nuevos en este archivo (mismo criterio que en el resto del sitio: solo decorativo, nunca texto plano) y con `prefers-reduced-motion` respetado (desactiva las animaciones del fondo y del botón, sin afectar la funcionalidad).

**Bug encontrado y corregido durante la verificación:** el ícono del campo de documento quedaba superpuesto con el texto del placeholder — la regla `.input-icon-wrap input { padding-left: 38px }` perdía contra la regla genérica `input[type="text"] { padding: 12px 14px }` por especificidad CSS empatada y orden de aparición (el shorthand posterior ganaba y reseteaba el padding-left). Se corrigió subiendo la especificidad a `.input-icon-wrap input[type="text"]`.

**Verificado con un navegador real (Playwright):** cero errores de consola en las 6 páginas del sitio (solo se tocó `cliente/`, pero se corrió el chequeo completo por las dudas); el selector segmentado cambia de estado correctamente al hacer clic y el valor se usa bien en el login; el login completo con datos mockeados sigue funcionando igual que antes (mismo saldo, misma llave); al cerrar sesión el selector vuelve a CC; con `prefers-reduced-motion: reduce` emulado, las manchas de fondo no animan.

**Sub-tarea, quitar datos que parecían expuestos y ocultar el portal de comercios del sitio público — hecho y verificado, 27 ago.** Jhey vio la tarjeta de saldo del hero del home (`index.html`) y la interpretó como datos reales de un cliente expuestos públicamente — era una tarjeta ilustrativa con datos ficticios ("jheison68", 12.450 puntos), pero se veía igual de específica y creíble que un dato real, lo cual es un problema de presentación aunque no fuera un problema de seguridad real. Se corrigió enmascarando el saldo y la llave (ahora se ven como `•• •••` y `•••••••`), se le agregó una etiqueta explícita "Vista de ejemplo" en la esquina de la tarjeta, y el texto de abajo ahora dice "Tus puntos son privados hasta que inicias sesión" — refuerza el mensaje en vez de contradecirlo.

Jhey también pidió dejar de mostrar públicamente el portal de comercios (`comercio/index.html`): hoy ya usa Supabase Auth (email/contraseña) y no expone nada sin login válido, pero va a convertirse en un dashboard dedicado (como `admin/index.html`) y por ahora no debe aparecer como una opción navegable del sitio público. Se quitaron los 8 enlaces públicos que apuntaban a `/comercio/` (barra de utilidad de `index.html` y `aliados/index.html`, botones "Ya soy aliado" del nav y del hero en `aliados/index.html`, el párrafo "¿ya tienes tu negocio afiliado?" en el home, y los pies de página de `index.html`, `aliados/index.html` y `legal/index.html`). El archivo `comercio/index.html` no se tocó ni se eliminó — sigue funcionando igual, solo dejó de estar enlazado desde la navegación pública. La ruta directa sigue existiendo (no se implementó ningún bloqueo nuevo de acceso — eso es trabajo de seguridad real, no de "ocultar del menú").

**Verificado con un navegador real (Playwright):** cero errores de consola en las 5 páginas tocadas; búsqueda de `href="/comercio/"` en el DOM renderizado de las 5 páginas confirma cero enlaces restantes.

**Sub-tarea, billetera prefondeada por comercio + asignación manual de puntos — hecho y verificado, 27 ago.** Decisión de Jhey: en vez de construir ahora la integración servidor-a-servidor con Talleres (u otros negocios del ecosistema), cualquier comercio puede entrar a Puntos Neggo y asignarle puntos a un cliente por su llave, según el valor real de su compra. Para no emitir puntos "a crédito", el comercio necesita una billetera prefondeada: recarga (por fuera del sistema, transferencia manual) y Jhey confirma esa recarga desde el panel admin — mismo patrón manual que ya se usa para pagarle a un comercio solo-canje sus canjes.

**Antes de escribir cualquier SQL** se revisó el esquema real en Supabase (no de memoria): `otorgar_puntos()` ya existía pero está atada a un `valor_compra` real reportado por un producto integrado (Neggo/Talleres) — no sirve para que un comercio invente un monto de puntos directo, por eso se creó una función separada. También se confirmó la tasa que ya usa el resto del sistema para no inventar una nueva: 800 COP de compra = 1 punto ganado (la misma que usa `otorgar_puntos`), y 1 punto = 10 COP de valor (la misma que usa `solicitar_canje` para calcularle la deuda a un comercio) — la billetera se descuenta con esa segunda tasa.

**Migración `billetera_comercio_y_asignacion_manual`:** 2 columnas nuevas en `comercios_solo_canje` (`saldo_billetera_cop`, y `campana_activa_nombre`/`campana_activa_multiplicador` para un único multiplicador activo por comercio, sin fechas ni historial — Jhey lo administra directo en la base por ahora, sin panel propio todavía, mismo criterio que ya se usa para dar de alta un comercio sin portal). Tabla nueva `comercio_recargas` (ledger append-only, RLS: el comercio ve solo las suyas, admin ve todas, nadie tiene INSERT directo). Tres funciones `SECURITY DEFINER` nuevas, todas con `SET search_path = public` y revocado el acceso a `anon`: `admin_recargar_billetera_comercio` (solo `is_admin()`), `comercio_portal_buscar_cliente` (busca por llave, devuelve nombre + teléfono enmascarado — nunca el documento ni el teléfono completo salen de la base), y `comercio_portal_otorgar_puntos` (valida comercio activo vía `mi_comercio_id()`, cliente no congelado, billetera alcanza — si no alcanza falla sin dejar ningún cambio a medias, aplica el multiplicador de campaña si el comercio tiene una activa, descuenta la billetera y acredita los puntos al cliente).

**Formulario en `comercio/index.html`** ("Asignar puntos a un cliente"): llave del cliente → al salir del campo busca y muestra nombre + últimos 4 dígitos del celular como confirmación antes de asignar (así el comercio ve que es la persona correcta) → un campo "Campaña" que muestra "No aplica campaña" o "Sí aplica: <nombre> (x<multiplicador>)" según lo que tenga configurado el comercio, de solo lectura → valor de la compra → el sistema calcula los puntos y los asigna. Se agregó también una tarjeta "Tu billetera" mostrando el saldo disponible. **Notificación al cliente:** por ahora el movimiento queda en su historial con fecha y hora (visible la próxima vez que entra a Puntos Neggo) — sirve como prueba ante un reclamo. La notificación por SMS que pidió Jhey queda pendiente: hoy el sistema no tiene ningún proveedor de SMS conectado (ni Twilio ni otro) — es una gestión/decisión suya antes de poder construirla.

**En `admin/index.html`:** columna nueva "Billetera" en la tabla de comercios con el saldo y un formulario chico para recargar (pide confirmación de que la plata ya llegó, mismo patrón que "marcar canje pagado"), y columna "Campaña" de solo lectura.

**Verificado con datos y llamadas reales contra Supabase (creados y limpiados después, nunca se tocó el comercio real "talleres"):** recarga de billetera correcta ($50.000 → billetera en $50.000); asignación sin campaña con compra de $16.000 → 20 puntos, billetera baja a $49.800 (200 = 20×10); con campaña activa x3, misma compra → 60 puntos, billetera baja $600; intento de asignar más de lo que alcanza la billetera falla limpio y **no cambia nada** (verificado que la billetera quedó exactamente igual después del error); llave inexistente falla con mensaje claro; usuario sin comercio vinculado no puede llamar la función; usuario no-admin no puede recargar billeteras ajenas; cliente congelado bloquea la asignación. Se corrió `get_advisors(security)` después de la migración: las únicas alertas nuevas son del mismo tipo que ya tienen todas las funciones equivalentes de este proyecto (ej. `marcar_canje_pagado`, `confirmar_canje_comercio`) — funciones que dependen de `is_admin()`/`mi_comercio_id()` en vez de revocar `authenticated` por completo; no es una alerta nueva de este cambio, es el patrón ya aceptado en todo el repo. También se verificó el frontend con Playwright (comercio + admin): cero errores de consola, el formulario de asignación calcula y muestra bien, la confirmación de identidad del cliente aparece antes de poder asignar, y la recarga desde admin actualiza el saldo visible al instante.

**Hallazgo aparte, resuelto el mismo día — 27 ago.** La tabla `admins` estaba vacía en producción: ningún usuario real podía pasar `is_admin()`, así que ninguna acción de admin (marcar canjes pagados, congelar clientes, recargar billeteras) funcionaba. Jhey pidió acceso con su cuenta `jf.neggo@gmail.com` tanto de admin como de comercio para poder revisar todo. Se resolvió como una operación de datos simple (no una migración): se insertó su `user_id` en `admins`, y se vinculó esa misma cuenta como dueña del comercio real "talleres" (`comercios_solo_canje.user_id`) — el mismo patrón manual que ya usa el propio panel admin para dar de alta un comercio sin portal propio. No se creó ni se reseteó ninguna contraseña — la cuenta ya tenía credenciales funcionando de pruebas anteriores. Verificado con `SELECT`: `es_admin = true` y `comercio_vinculado = 'talleres'`. Con esto, `jf.neggo@gmail.com` ya puede entrar a `/admin/` y a `/comercio/` con su contraseña de siempre.

**Sub-tarea, capa de seguridad anti fuerza bruta en el login de comercios — hecho y verificado, 27 ago.** Jhey pidió reforzar específicamente el login de `comercio/index.html` (ya usaba Supabase Auth: contraseña con hash seguro, sesión con token firmado, HTTPS). Se agregó una capa de bloqueo temporal + auditoría, con una limitación real que se documenta abajo para que quede clara.

**Diseño:** tabla nueva `comercio_login_intentos` (auditoría append-only, solo admin puede leerla). Tres funciones `SECURITY DEFINER` nuevas: `comercio_login_verificar_bloqueo(email)` (consulta si un email tiene 5+ fallos en los últimos 15 minutos — si sí, el frontend ni intenta el login y muestra cuántos minutos faltan), `comercio_login_registrar_fallo(email)` (registra un fallo — tiene que ser invocable sin sesión, porque cuando un login falla todavía no hay sesión), y `comercio_login_registrar_exito()` (registra un éxito, pero a diferencia de la de fallo, esta lee el email desde la sesión autenticada real —`auth.uid()`— nunca de un parámetro que mande el navegador, así que no se puede falsificar). El frontend de `comercio/index.html` ahora: 1) antes de enviar el formulario, pregunta si el email está bloqueado; 2) si el login falla, registra el fallo; 3) si el login tiene éxito, registra el éxito y limpia el contador implícitamente (la ventana de 15 min expira sola).

**Limitación real, documentada a propósito:** la función de registrar fallos es invocable sin sesión (no hay otra forma, ya que el login todavía no pasó). Eso significa que alguien podría llamarla directo (sin pasar por el formulario) con el email de un comercio ajeno para forzarle 15 minutos de bloqueo — no puede robarle la cuenta ni ver ningún dato, solo demorarle el login un rato. Es una limitación conocida de cualquier bloqueo hecho a nivel de aplicación sin un proxy de autenticación real. La forma completa de cerrar esto es activar CAPTCHA en el login (Supabase lo soporta nativo con hCaptcha o Cloudflare Turnstile) — queda como mejora aparte, no la hice porque requiere que Jhey cree una cuenta en uno de esos dos proveedores primero.

**3 ajustes que solo Jhey puede activar** (Authentication → dashboard de Supabase, no son cosas que se hagan por SQL):
1. **Protección de contraseñas filtradas** (Authentication → Policies → Password): activa el chequeo contra HaveIBeenPwned. Hoy está desactivado — lo confirmó `get_advisors(security)`.
2. **Política de contraseña fuerte** (mismo lugar): largo mínimo y complejidad.
3. **Límite de tasa de Auth** (Authentication → Rate Limits): límite de intentos por IP a nivel de toda la plataforma, complementa el bloqueo por email que ya se armó.

**Verificado con navegador real (Playwright):** 5 intentos fallidos seguidos → el 6to queda bloqueado con el mensaje correcto y no llega a intentar `signInWithPassword` (ni con la contraseña correcta); un email distinto no se ve afectado por el bloqueo de otro; cero errores de consola. Se limpiaron los datos de prueba de la tabla `comercio_login_intentos` después de verificar. `get_advisors(security)` después de la migración: mismas alertas esperadas (funciones que dependen de `is_admin()`o de sesión en vez de revocar `authenticated` del todo — patrón ya aceptado en el resto del repo) más la de "leaked password protection" que ya existía antes de este cambio.

**Sub-tarea, cuenta de cliente de prueba "jheison68" — hecho, 27 ago.** A pedido de Jhey, se creó un registro en `clientes_puntos` para que pueda probar el sistema como cliente. Nombre real (Jheison Arley Castaño Flórez) y contacto ya conocidos de la conversación; el número de documento es un número de prueba inventado (no es su cédula real — no tengo forma de traerla automáticamente: "tallergo" no es uno de los proyectos de Supabase conectados a esta sesión, y la integración servidor-a-servidor entre productos quedó explícitamente pospuesta). La llave sale del nombre + los últimos dígitos del documento, así que se eligió un número que termina en 68 a propósito para que la llave diera exactamente `jheison68`.

- Tipo/número de documento para loguearse en `cliente/index.html`: **CC 1017000068**
- Llave (la que usa un comercio para asignarle puntos): **jheison68**
- Saldo inicial: 0 puntos (no se le asignó nada de arranque — si Jhey quiere probar con saldo ya cargado, hay que asignárselo desde el portal de comercios o pedírmelo).

**Bug encontrado y corregido, 27 ago (reportado por Jhey probando en vivo).** Las 5 funciones del portal de cliente (`cliente_portal_estado`, `cliente_portal_buscar_destinatario`, `cliente_portal_comercios_disponibles`, `cliente_portal_solicitar_canje`, `cliente_portal_transferir`) solo tenían permiso de ejecución para el rol `anon` (visitante sin sesión) — nunca se les dio permiso a `authenticated`. Ninguna de las 5 depende de `auth.uid()` ni de ningún dato de sesión, se identifican 100% por documento/llave (así está diseñado, los clientes no tienen cuenta de Supabase Auth). El problema apareció porque Jhey tenía sesión abierta como comercio/admin en el mismo navegador al entrar a `cliente/index.html` — la llamada viajó como `authenticated` en vez de `anon` y Postgres la rechazó con "permission denied". Se corrigió dándole `EXECUTE` también a `authenticated` en las 5 funciones (migración `fix_cliente_portal_grants_authenticated`) — verificado con una sesión autenticada simulada, ya devuelve el saldo real. Este bug afecta a cualquier persona que sea comercio/admin y también cliente a la vez (como Jhey), no solo a pruebas.

**Sub-tarea, 3 pedidos de Jhey probando en vivo — hecho y verificado, 27 ago.**

**1. Transferir puntos entre clientes:** confirmado por Jhey que funciona bien probando como cliente. No se tocó nada.

**2. "Elige un comercio" aparecía vacío al canjear + llave de comercio:** la lista vacía era el mismo bug de permisos de `cliente_portal_comercios_disponibles` que ya se había corregido un poco antes (ver arriba, "permission denied for function cliente_portal_estado") — verificado de nuevo, ya devuelve "talleres" con sesión autenticada. Además se agregó lo que pidió Jhey: los comercios ahora tienen su propia **llave** (nombre normalizado + 2 dígitos aleatorios, ej. `talleres95`), igual espíritu que la llave de cliente pero sin depender de un documento. Se generó automáticamente para "talleres" y queda visible para el propio comercio (tarjeta "Tu negocio" en `comercio/index.html`) y para el admin (columna nueva "Llave" en la tabla de comercios). Migración `llave_comercio_y_redencion_en_persona`: columna `llave_comercio` (única) + función `_generar_llave_comercio` + trigger `trg_generar_llave_comercio` (mismo patrón que la llave de cliente).

**3. Redención en persona en cualquier comercio (parcial o total, sin código):** hasta ahora la única forma de redimir puntos era que el cliente la pidiera desde su propio portal (`cliente_portal_solicitar_canje`) y le mostrara un código al comercio — pensado para cuando el cliente puede estar en cualquier lado. Pero cuando el cliente ya está physically en el negocio pagando, ese código no aporta nada (el comercio ya lo tiene enfrente, es su propia confirmación). Se agregó una función nueva `comercio_portal_redimir_puntos(llave_cliente, puntos, motivo)` — mismo estilo que "Asignar puntos": el comercio escribe la llave del cliente, ve su nombre + saldo disponible, escribe cuántos puntos quiere redimir (mínimo 200, hasta el saldo del cliente) y listo — sin código de por medio, porque el canje queda auto-confirmado (`confirmado_comercio_at = now()`) en el mismo momento. El resto de la plata (si el cliente paga una parte con puntos y otra en efectivo) la cobra el comercio directo, por fuera del sistema — Puntos Neggo solo trackea la parte en puntos. Ese redimido queda igual que cualquier otro canje: "pendiente de pago" hasta que Jhey le transfiera la plata al comercio y lo marque pagado desde el admin. Esta opción queda disponible para **todos** los comercios activos por igual (no hay todavía una distinción de "solo asigna" / "solo redime" / "hace las dos cosas" — se puede agregar después si Jhey define que algún comercio necesita quedar restringido a una sola).

También se aprovechó para que `comercio_portal_buscar_cliente` devuelva el saldo del cliente (antes solo devolvía nombre y teléfono enmascarado) — lo necesita la vista de redención para no dejar que el comercio intente redimir más de lo que el cliente tiene, y de paso se muestra también en "Asignar puntos" como dato útil.

**Verificado con datos reales contra Supabase (creados y limpiados después, sin tocar a "talleres" ni a jheison68):** cliente de prueba con 500 puntos → redimir 300 → saldo baja a 200, canje queda `pendiente_pago` con `confirmado_comercio_at` ya puesto, `valor_cop` = 3.000 (300 × 10, la misma tasa de siempre); intento de redimir 10.000 con solo 200 disponibles falla limpio sin dejar cambios a medias. `get_advisors(security)` después de la migración: mismo patrón de alertas ya aceptado en el resto del repo, sin sorpresas nuevas. Verificado también con navegador real (Playwright) en `comercio/index.html` y `admin/index.html`: la llave se ve en ambos paneles, el flujo de redención completo funciona (mínimo 200, bloqueo cliente-side si pide más de lo que el cliente tiene, mensaje de éxito con el valor en pesos), cero errores de consola.

**Sub-tarea, historial de movimientos + código vigente + canjear desde el comercio con confirmación — hecho y verificado, 27 ago.** Jhey pidió varias cosas relacionadas después de probar en vivo:

**1. Historial del cliente, ahora descrito por concepto.** El historial ya existía (`cliente_portal_estado` ya devolvía `movimientos`), pero solo mostraba una etiqueta genérica ("Puntos ganados", "Canje", etc.) sin decir en qué comercio ni con quién. Ahora cada movimiento dice: "Puntos ganados en talleres", "Canjeado en talleres", "Transferido a Carlos Ruiz", "Recibido de Carlos Ruiz". Para las transferencias hubo que agregar una columna nueva `contraparte_cliente_id` en `puntos_movimientos` (antes no se guardaba quién era la otra punta de una transferencia) y completarla en `transferir_puntos` — cambio aditivo, no toca ninguna validación de esa función ya probada.

**2. Código de 6 dígitos vigente hasta que el comercio lo acepte.** Antes el cliente solo veía el código una vez, justo al pedir el canje — si cerraba la pantalla, lo perdía. Ahora hay una sección nueva "Canjes pendientes" en `cliente/index.html` (arriba del historial) que muestra todo canje sin confirmar con su código, comercio y puntos — vía la función nueva `cliente_portal_canjes_pendientes`. Se actualiza sola al pedir un canje nuevo y al volver a entrar.

**3. El comercio también puede iniciar un canje — mismo proceso, con confirmación del cliente.** Corrección importante sobre lo que se había construido antes: la función `comercio_portal_redimir_puntos` (la de "Redimir puntos de un cliente" en el portal de comercio) ya NO se auto-confirma. Antes, como el comercio ya tenía al cliente en frente, quedaba confirmado al instante — pero Jhey pidió que el cliente confirme igual con el código, sin importar quién inició el canje. Ahora: el comercio escribe la llave y los puntos, el sistema genera el mismo código de 6 dígitos de siempre (el comercio nunca lo ve — si lo viera, confirmar no serviría de nada), el cliente lo ve en su cuenta (sección "Canjes pendientes"), se lo dice al comercio, y el comercio lo confirma en la misma sección "Canjes por confirmar" que ya existía — un solo mecanismo de confirmación para los dos orígenes (cliente remoto o comercio en persona), no dos sistemas distintos.

**4. Historial del comercio.** Sección nueva en `comercio/index.html` mostrando, por cada movimiento del negocio: cliente, puntos y valor en pesos — tanto lo que asignó como lo que se redimió en su negocio (con su estado: pendiente de pago / pagado / cancelado). Función nueva `comercio_portal_historial`.

**Verificado con datos reales contra Supabase (creados y limpiados después, sin tocar "talleres" ni jheison68, billetera del comercio restaurada a su valor original $13.750):** transferencia de 30 puntos entre dos clientes de prueba → el historial de cada uno muestra el nombre correcto de la contraparte en ambos sentidos; puntos ganados muestran "talleres" como comercio; redención iniciada por el comercio queda `pendiente_pago` con `confirmado_comercio_at` en null (ya no se auto-confirma) y el código no viaja en la respuesta al comercio; `cliente_portal_canjes_pendientes` muestra el código correcto; `confirmar_canje_comercio` con ese código confirma bien. `get_advisors(security)` después de la migración: mismo patrón aceptado en todo el repo, sin sorpresas. Verificado también con navegador real (Playwright) en `cliente/index.html` (sección de canjes pendientes con código, historial con conceptos descriptivos) y `comercio/index.html` (historial de movimientos con cliente/puntos/valor/estado) — cero errores de consola en ambos.

**Sub-tarea, código flexible + vencimiento de 30 minutos + campaña autogestionada + rediseño en pestañas — hecho y verificado, 27 ago.** Jhey probó en vivo y pidió 4 cosas.

**1. Código de verificación sin importar el espacio.** El código se guarda formateado "123 456" (así se ve más legible en la app del cliente), pero cuando el comercio lo escribe a mano copiándolo dígito por dígito, es normal que lo escriba sin el espacio ("123456") y antes eso fallaba con "Código de verificación incorrecto". Se corrigió `confirmar_canje_comercio` para comparar ignorando espacios y mayúsculas/minúsculas — el código se sigue guardando y mostrando igual que siempre, solo cambió cómo se compara.

**2. Vencimiento de 30 minutos por canje.** Columna nueva `canjes.expira_at` (se calcula solo, `created_at` + 30 minutos). Como este proyecto no tiene cron ni scheduled jobs, el vencimiento se resuelve de forma perezosa: una función interna `_expirar_canje` revisa si un canje sin confirmar ya venció, y si es así cancela el canje y le devuelve los puntos al cliente — esa función se llama automáticamente desde los tres lugares donde alguien puede toparse con un canje pendiente: cuando el cliente ve sus "Canjes pendientes", cuando el comercio ve sus "Canjes por confirmar", y cuando el comercio intenta confirmar uno puntual.

**Bug real encontrado en la verificación (no en producción, se agarró antes de publicar):** la primera versión de `confirmar_canje_comercio` revertía los puntos y *después* lanzaba un error diciendo "este canje venció" — pero un error sin capturar en Postgres deshace toda la transacción de esa llamada, así que el reverso de puntos se deshacía junto con el error y el cliente se quedaba sin sus puntos. Se corrigió: ahora la función devuelve un resultado normal con `vencido: true` en vez de lanzar un error para ese caso puntual, así el reverso de puntos queda confirmado de verdad. Se verificó explícitamente con un canje vencido a propósito (adelantando su `expira_at`) que el saldo del cliente queda bien devuelto y se mantiene después de la respuesta — antes del arreglo se perdía, después del arreglo no.

**3. Campaña autogestionada por el comercio.** Antes "Campaña activa" era solo un texto de solo lectura — no había ninguna forma de activarla o cambiarla salvo que Jhey lo hiciera directo en la base de datos. Ahora `comercio/index.html` tiene una sección "Campaña" con nombre + multiplicador (2x a 5x, para evitar errores de digitación) y botones "Activar/actualizar" y "Desactivar" — funciones nuevas `comercio_portal_actualizar_campana` y `comercio_portal_desactivar_campana`. Se permite que cada comercio lo autogestione sin aprobación de Jhey porque el costo extra de puntos siempre sale de la propia billetera de ese comercio — no hay ningún riesgo para otros comercios ni para el sistema en general.

**4. Rediseño en pestañas (antes todo apilado hacia abajo).** En `comercio/index.html`, "Canjes por confirmar" e "Historial de movimientos" ahora comparten una sola sección "Actividad de canjes" con un selector tipo pestaña — se ve solo lo que corresponde a la pestaña activa, con un contador chico en "Canjes por confirmar" cuando hay alguno esperando. En `cliente/index.html`, "Historial" y "Canjes pendientes" tienen el mismo tratamiento (reutilizando el mismo selector segmentado que ya se usaba para elegir el tipo de documento en el login) — y si el cliente tiene algún canje pendiente al entrar, esa pestaña se abre sola porque es lo que necesita su atención. Cada canje pendiente ahora también muestra cuánto le falta para vencer ("Vence en 18 min"), en naranja normal y en rojo cuando quedan 5 minutos o menos.

**Verificado con datos reales contra Supabase (creados y limpiados después, sin tocar "talleres" ni jheison68, saldo del cliente restaurado a su valor original 2.398):** código escrito sin espacio confirma correctamente; canje vencido a propósito (adelantando `expira_at`) se cancela solo y devuelve los puntos en los tres puntos de contacto (lista del cliente, lista del comercio, intento de confirmación) — incluyendo la verificación puntual de que el reverso sobrevive a la respuesta después del arreglo del bug; intento de confirmar dos veces el mismo canje falla con el mensaje correcto; campaña se activa, se ve reflejada de inmediato, y se desactiva bien; multiplicador fuera de rango (1) y nombre vacío fallan con mensajes claros; una sesión sin comercio vinculado no puede tocar la campaña de nadie. `get_advisors(security)` después de las migraciones: mismo patrón de alertas ya aceptado en todo el repo, sin categorías nuevas. Verificado también con navegador real (Playwright) en ambos portales: pestañas cambian bien, el contador y el aviso de vencimiento se ven correctos, la confirmación de un canje vencido muestra el mensaje amistoso sin marcarlo como error de sistema, campaña se activa/desactiva desde la UI — cero errores de consola en las dos páginas.

**Sub-tarea, campañas múltiples por comercio + cuenta del comercio reorganizada + panel admin reorganizado para escala — hecho y verificado, 28 ago.** Jhey pidió reorganizar toda la sesión de comercios (muy confusa: puntos que asigna y puntos que le deben mezclados en un solo historial) y corregir el modelo de campañas (antes solo una activa a la vez, sin poder elegir a quién le aplica) — más limpiar el panel admin para cuando haya miles de comercios.

**Decisión de arquitectura, confirmada por Jhey antes de construir:** un comercio puede tener varias campañas activas a la vez (ej. una "todos" y otra solo para "clientes nuevos"), pero la selección de cuál le aplica a cada cliente **la hace el comercio manualmente** con un desplegable al momento de asignar puntos — no hay detección automática de "es cliente nuevo o recurrente" todavía (eso queda para más adelante, es solo una etiqueta informativa en el segmento por ahora, sin lógica automática detrás).

**1. Campañas múltiples.** Se reemplazó el modelo viejo (una sola campaña por comercio, columnas `campana_activa_nombre`/`campana_activa_multiplicador` directo en `comercios_solo_canje`) por una tabla nueva `comercio_campanas` (nombre, multiplicador 2-5, segmento `todos`/`nuevo`/`recurrente`, activa/inactiva) — un comercio puede tener hasta 5 campañas activas al mismo tiempo. Las columnas viejas se dejaron en la base (sin usarlas más, no se borraron por seguridad) — cualquier campaña vieja quedó migrada a una fila en la tabla nueva. Funciones nuevas: `comercio_portal_listar_campanas`, `comercio_portal_crear_campana`, `comercio_portal_desactivar_campana`. En "Asignar puntos" ahora hay un desplegable "Campaña que le aplica a este cliente (opcional)" con las campañas activas del comercio — si no elige ninguna, el punto se asigna normal (x1). `comercio_portal_otorgar_puntos` ahora recibe cuál campaña eligió el comercio (o ninguna) en vez de leer una campaña fija del comercio.

**2. Cuenta del comercio con Puntos Neggo, reorganizada en 3 partes claras.** Antes todo el historial de canjes (por confirmar, ya confirmados, ya pagados) estaba mezclado en una sola lista, y encima mezclado visualmente con "puntos que el propio comercio asignó" — lo que hacía muy difícil para un comercio entender "¿qué me deben, qué ya me pagaron, qué es solo mi propio registro?". Ahora hay una sección "Tu cuenta con Puntos Neggo" con 3 tarjetas de resumen (Te debemos / Ya te pagamos / Por confirmar) y 3 pestañas debajo con el detalle de cada una — funciones nuevas `comercio_portal_resumen_cuenta` y `comercio_portal_canjes_pagados`. Los "puntos que el comercio le ha asignado a sus clientes" quedaron en una sección aparte y separada ("Actividad — puntos que has asignado"), con una aclaración explícita de que eso no es dinero que le deban.

**3. "Tu negocio" + "Tu billetera" fusionados en una sola tarjeta.** Antes eran dos bloques separados; ahora es una sola tarjeta visual con el nombre, la llave y el saldo de la billetera (lo que tiene disponible para seguir asignando puntos) en un solo lugar, más prominente.

**4. Panel admin — "Canjes pendientes de pago" corregido y con selección múltiple.** Jhey notó que esta tabla mostraba una columna "Código" que el comercio nunca ve por ningún lado (es correcto que no la vea — el código es solo para que el cliente se lo dé al comercio en persona, el admin no lo necesita) y además mostraba canjes que el comercio **todavía no había confirmado** con el cliente, que no son pagables todavía. Se corrigió: la tabla ahora solo muestra canjes que el comercio ya confirmó (columna "Confirmado" con la fecha, reemplazando la columna "Código" que se quitó), y se agregó una casilla de selección por fila + "seleccionar todos", con una barra que muestra en vivo la cantidad, los puntos y el saldo total de lo seleccionado, y un botón "Marcar seleccionados como pagados" (función nueva `admin_marcar_canjes_pagados`, tolera que algún id falle sin bloquear el resto del lote y reporta cuáles).

**5. Panel admin — "Pasivo financiero" separado en dos: clientes vs. comercios.** Antes "Puntos en circulación" (lo que se les debe a los *clientes* en puntos) y "Cuenta por pagar por comercio" (lo que se les debe a los *comercios* en dinero) se veían como si fueran la misma cosa, uno debajo del otro sin separación visual clara — de ahí la confusión de Jhey. Ahora son dos tarjetas lado a lado con títulos explícitos ("Clientes — puntos en circulación" / "Comercios — cuentas por pagar"), cada una con sus propios totales — la de comercios ahora también muestra el total agregado (valor total pendiente, puntos pendientes, cantidad de comercios con saldo pendiente), calculado sobre la tabla de abajo.

**6. Panel admin — "Cuenta por pagar por comercio" preparada para escala.** Se agregó un buscador (filtra por nombre de comercio, pensado para cuando haya miles) y un botón "Ver cuenta" por comercio que despliega, sin salir de la tabla, el mismo desglose de 3 partes que ve el comercio en su propio portal (por confirmar / pendiente de pago / ya pagado) — función nueva `admin_comercio_cuenta(comercio_id)`, de solo lectura para el admin.

**7. Panel admin — columna "Campaña" corregida.** Como el modelo de campañas cambió a la tabla nueva `comercio_campanas`, la columna vieja (que leía directo `campana_activa_nombre`/`campana_activa_multiplicador`, ahora siempre vacías) se actualizó para mostrar las campañas activas reales de cada comercio como etiquetas ("Aniversario x3", "Bienvenida x2"), de solo lectura — cada comercio gestiona las suyas desde su propio portal.

**Bug encontrado y corregido en la verificación (mismo patrón que el de vencimiento del canje, esta vez en el panel admin):** al marcar canjes como pagados en bloque, el mensaje de éxito ("✓ N canjes marcados como pagados") se ponía en pantalla *antes* de recargar la tabla — pero la función que recarga la tabla limpia ese mismo mensaje al empezar, así que el mensaje desaparecía de inmediato aunque la operación sí hubiera funcionado. Se corrigió con el mismo orden ya usado antes: recargar primero, escribir el mensaje después.

**Verificado con datos reales contra Supabase (creado y limpiado después, sin tocar los 8 canjes reales pendientes de pago de "talleres" ni su historial):** `admin_comercio_cuenta` sobre "talleres" devuelve exactamente los mismos números que se ven hoy en el admin (8 canjes pendientes de pago, 2.399 puntos, $23.990 — coincide con lo que Jhey reportó estar viendo); `admin_marcar_canjes_pagados` con un canje de prueba + un id inventado en el mismo lote: el real queda pagado, el inventado reporta "no está en estado pagable" sin afectar al otro. `get_advisors(security)` después de las 3 migraciones nuevas: mismo patrón de alertas ya aceptado en el resto del repo, sin categorías nuevas. Verificado también con navegador real (Playwright) en ambos paneles: en `comercio/index.html` — tarjeta fusionada, campañas se crean/desactivan y aparecen en el desplegable de "Asignar puntos", asignar con campaña seleccionada aplica el multiplicador correcto, las 3 pestañas de "Tu cuenta" muestran los números correctos y se actualizan al confirmar un canje; en `admin/index.html` — pasivo dividido en dos tarjetas con sus totales, buscador de comercios funciona, "Ver cuenta" despliega el desglose correcto, selección múltiple + marcar pagados en bloque funciona (incluyendo que el mensaje de éxito ahora sí se queda en pantalla), columna de campañas muestra las campañas reales. Cero errores de consola en ambos paneles.

**Sub-tarea, bug del formulario público de afiliación + reorganización visual de los 3 paneles (cliente, comercio, admin) — hecho y verificado, 28 ago.** Jhey reportó que el formulario "Quiero afiliar mi negocio" fallaba con un mensaje genérico, y pidió organizar mejor el dashboard tanto de cliente como de comercio (con la sección "Actividad — puntos que has asignado" como ejemplo concreto), además de un panel admin "mucho más profesional" — incluyendo que "Canjes pendientes de pago" se vea organizado por comercio, pensando en que van a ser miles.

**1. Bug del formulario público — encontrado y corregido.** La tabla `solicitudes_comercio` (creada el 27 ago) solo tenía permiso de `INSERT` para el rol `anon` (visitante sin sesión). El bug: `aliados/index.html` usa el mismo cliente de Supabase que el resto del sitio, y si quien llena el formulario tiene en el mismo navegador una sesión abierta de comercio o admin (el localStorage de la sesión se comparte por origen), la llamada sale como rol `authenticated`, no `anon` — y `authenticated` no tenía permiso de insertar. Se confirmó el diagnóstico con una prueba directa contra Supabase simulando ambos roles: como `anon` el insert funciona, como `authenticated` fallaba con "permission denied for table solicitudes_comercio". Mismo patrón de bug ya visto antes en este proyecto (rol inesperado por sesión compartida, no dato inválido). Se corrigió agregando permiso de `INSERT` también para `authenticated` con la misma policy abierta que ya tenía `anon` (`with check (true)`) — la tabla es solo captura de leads, sin riesgo real en permitirlo. Migración `fix_solicitudes_comercio_insert_authenticated`. Verificado: insert simulando la sesión real de un comercio/admin ahora funciona; dato de prueba borrado después. `get_advisors(security)`: mismo patrón de alertas ya aceptado en el repo, sin categorías nuevas (esta tabla no usa `SECURITY DEFINER`, no genera alertas nuevas).

**2. Comercio — reorganización del dashboard.** Se agregó una barra de navegación rápida fija arriba (Resumen / Campañas / Operar / Tu cuenta / Actividad) que resalta la sección que se está viendo mientras se hace scroll (sin usar IntersectionObserver por un caso límite real: la banda superior típica de un scrollspy nunca se activa para la última sección si el contenido de abajo es corto — se optó por un cálculo simple en cada evento de scroll, más robusto). Las 5 secciones existentes se envolvieron en `<section id="...">` para que los enlaces salten directo ahí. La sección "Actividad — puntos que has asignado" —la que Jhey señaló como ejemplo— se rediseñó: 3 tarjetas de estadística arriba (asignaciones, puntos y clientes distintos del mes en curso, calculadas en el navegador sobre los datos ya cargados), un buscador por nombre de cliente, y las filas cambiaron del estilo genérico de canje a un estilo de movimiento con iniciales del cliente en una burbuja de color, nombre en primer plano, y puntos + valor en pesos alineados a la derecha — mismo lenguaje visual que ya usa el historial del cliente.

**3. Cliente — historial agrupado por fecha.** El historial de movimientos (`cliente/index.html`) ahora se agrupa bajo encabezados "Hoy" / "Ayer" / "Esta semana" / "Este mes" / "Antes" en vez de una lista plana — más fácil de escanear de un vistazo. El resto del panel de cliente ya estaba bien resuelto (tarjeta de saldo, acciones, pestañas historial/pendientes) y no se tocó más que esto.

**4. Admin — rediseño visual completo.** El panel admin usaba HTML prácticamente sin estilo (tablas con borde simple, botones azules planos) mientras que los otros dos paneles ya tenían un sistema de diseño coherente (fondo con degradado suave, tarjetas, marca "N" en gradiente, botones y tokens de color consistentes). Se le aplicó el mismo sistema de diseño: marca, tarjetas con sombra suave, tablas envueltas en tarjetas con encabezado en gris claro y hover por fila, badges en vez de texto de color plano, y la misma barra de navegación rápida con scrollspy que se agregó en comercio (Resumen / Cuenta por comercio / Canjes pendientes / Comercios / Solicitudes).

**5. Admin — "Canjes pendientes de pago" organizado por comercio.** Antes era una sola tabla plana con todos los canjes pagables mezclados, sin distinción visual de a qué comercio pertenece cada uno (con un solo comercio de prueba activo no se notaba, pero no iba a escalar a miles). Ahora se agrupan por nombre de comercio, cada grupo con su propio encabezado (nombre + subtotal de cantidad, puntos y valor de ese comercio) y su propia casilla "Seleccionar todos" — pero la selección sigue siendo una sola bolsa global, así que se puede pagar en un solo clic una mezcla de canjes de varios comercios distintos a la vez, tal como ya funcionaba antes. Se mantuvo intacta la corrección de la sesión anterior (recargar antes de mostrar el mensaje de éxito, para que no se borre solo).

**Verificado con navegador real (Playwright) en los tres paneles, cero errores de consola en todos los casos:** en `comercio/index.html` — la barra de navegación resalta la sección correcta al hacer clic y al hacer scroll (incluida la última sección, el caso límite que motivó no usar IntersectionObserver), las estadísticas de "Actividad" calculan bien el mes en curso excluyendo movimientos de meses anteriores, el buscador filtra por nombre y muestra el estado vacío correcto cuando no hay coincidencias; en `cliente/index.html` — el historial se agrupa correctamente en las 5 categorías de fecha esperadas; en `admin/index.html` — el drill-down "Ver cuenta" sigue funcionando, los canjes pendientes de pago se agrupan por comercio con los subtotales correctos, seleccionar todo un grupo con su checkbox marca exactamente esos canjes, se puede sumar selección de otro grupo y pagar los dos comercios en un solo clic, y el mensaje de éxito persiste después de la recarga. Ningún cambio de este bloque tocó tablas ni funciones de Supabase (fuera del fix puntual de `solicitudes_comercio`) — es reorganización visual sobre el mismo backend ya verificado.

**Sub-tarea, login real de cliente (cierra la deuda de seguridad documentada desde el 27) + activación self-service de comercios + arreglo del bug "la solicitud aceptada desaparecía" + bandera solo-canje — hecho y verificado, 28 ago.** Jhey pidió 2 mejoras y reportó 1 bug/1 pedido de mejora al probar el panel admin en vivo. Este era exactamente el trabajo pendiente que se documentó como deuda explícita en la sub-tarea del 27 ("identificamos solo con tu documento... el PIN se pospone").

**1. Login real de cliente con Supabase Auth (reemplaza la identificación por solo-documento).** `cliente/index.html` pasó de "escribe tu documento y ya" a un login real: pestañas "Iniciar sesión" / "Crear cuenta" en la misma tarjeta (se mantuvo el fondo aurora y el resto del diseño visual del 27, solo cambió el contenido del formulario). Crear cuenta pide nombres y apellidos, tipo/número de documento, correo, celular, ciudad, contraseña + confirmación, y una casilla de aceptación de términos/datos personales — más recuperación de contraseña (`auth.resetPasswordForEmail` + pantalla que detecta el enlace de recuperación vía el evento `PASSWORD_RECOVERY` y pide la contraseña nueva).

**Columnas nuevas:** `clientes_puntos.user_id` (uuid, `references auth.users(id)`, único vía índice parcial) y `clientes_puntos.ciudad`. Función nueva `mi_cliente_id()` (mismo patrón que el ya existente `mi_comercio_id()`).

**Función nueva `cliente_portal_registrar(...)`:** se llama después de `auth.signUp()` (ya con sesión). Si ya existe una fila de `clientes_puntos` con ese documento sin dueño (creada antes por `otorgar_puntos` servidor-a-servidor), la reclama en vez de duplicar — conserva el `id`, el saldo y todo el historial ya acumulado. Si esa fila ya tiene OTRO dueño, rechaza con "Ese documento ya está vinculado a otra cuenta" (anti-secuestro de cuenta). Función auxiliar `cliente_portal_documento_disponible(...)` (sin exponer datos, solo un booleano) para avisarle al usuario ANTES de crear la cuenta de Auth si el documento ya tiene dueño.

**El cambio de seguridad real, el motivo de fondo de esta sub-tarea:** las 5 funciones `cliente_portal_*` venían identificando al cliente por el `tipo_documento`/`numero_documento` que mandaba el propio navegador — con eso, cualquiera que conociera el documento de otra persona podía ver su saldo o mover sus puntos (el riesgo que ya se había documentado explícitamente el 27). Se migraron `cliente_portal_estado`, `cliente_portal_canjes_pendientes`, `cliente_portal_solicitar_canje` y `cliente_portal_transferir` para que ya no reciban esos parámetros — ahora resuelven la identidad del lado del servidor con `mi_cliente_id()` (`auth.uid()` de la sesión real), igual que ya hacía el portal de comercios. Las funciones internas ya probadas (`solicitar_canje`, `transferir_puntos`) no se tocaron — los wrappers del portal les siguen pasando el documento, solo que ahora lo leen de la fila del cliente autenticado, nunca de un parámetro del navegador.

**Bug real encontrado y corregido durante la verificación (dos veces, dos causas distintas):**
- `comercio_portal_activar` fallaba con "column reference nombre is ambiguous" — el nombre de columna de la tabla chocaba con el parámetro de salida de `RETURNS TABLE(..., nombre text)` (mismo patrón de bug que ya mordió este proyecto varias veces antes). Corregido calificando la tabla con alias antes de que llegara a producción.
- Más serio: después de migrar las 4 funciones con `DROP FUNCTION` + `CREATE FUNCTION` y revocar `EXECUTE` de `PUBLIC`, una verificación directa contra `information_schema.routine_privileges` mostró que el rol `anon` **seguía** teniendo `EXECUTE` en todas ellas. Causa real: este proyecto de Supabase tiene un `ALTER DEFAULT PRIVILEGES` que le da `EXECUTE` a `anon`/`authenticated` automáticamente en **toda función nueva** creada en el esquema `public` (confirmado consultando `pg_default_acl` directamente) — es un grant explícito al rol `anon`, no al pseudo-rol `PUBLIC`, así que `revoke all ... from public` no lo tocaba. Se corrigió con `revoke execute ... from anon` explícito en las 7 funciones nuevas/migradas que debían quedar solo para `authenticated` (`mi_cliente_id`, las 4 de `cliente_portal_*` migradas, `cliente_portal_registrar`, `comercio_portal_activar`) — re-verificado contra `information_schema.routine_privileges`, ya sin `anon` en ninguna. Este mismo default de proyecto puede volver a morder cualquier función nueva futura si solo se revoca de `PUBLIC` — queda anotado acá para no repetir el error.

**2. Activación self-service de comercios (decisión de diseño que Jhey delegó explícitamente).** Se evaluaron 2 opciones: contraseña temporal enviada por correo, o que el propio comercio la cree la primera vez. Se eligió la segunda — self-service por email, sin contraseña temporal — porque este proyecto no tiene ningún proveedor de envío de correo propio conectado todavía (la contraseña temporal necesitaría infraestructura que no existe), y porque es el mismo patrón ya usado y ya probado para la recuperación de contraseña de cliente/comercio (Supabase ya envía esos correos).

**Columnas nuevas en `comercios_solo_canje`:** `email` (estructurado, distinto del `contacto` de texto libre que ya existía, único vía índice parcial case-insensitive) y `solo_canje` (boolean, default `false`, ver punto 4). También se cerró un hueco que existía desde antes: `comercios_solo_canje.user_id` tenía el `FOREIGN KEY` hacia `auth.users` pero nunca tuvo `UNIQUE` — se agregó el índice único (parcial, ignora `NULL`) porque la activación self-service depende de que una cuenta de Auth no pueda quedar vinculada a dos comercios a la vez.

**Funciones nuevas:** `comercio_portal_verificar_activacion(email)` (booleano, sin exponer datos — ¿hay un comercio pendiente con ese correo?) y `comercio_portal_activar(email)` (requiere sesión ya creada vía `auth.signUp()`, vincula `user_id` al comercio pendiente que coincida por correo, rechaza si el comercio ya fue activado por alguien más — mismo candado anti-carrera que usa el resto del proyecto, con `for update`). En `comercio/index.html` se agregó, en la pantalla de login, un enlace "¿Tu negocio fue aceptado? Activa tu cuenta" que lleva a: escribir el correo → si está pendiente, crear y confirmar contraseña → sesión creada automáticamente. También se agregó "¿Olvidaste tu contraseña?" (no existía antes, aunque el comercio ya usaba contraseña real desde el 20 de agosto).

**3. Arreglo del bug reportado ("acepté la solicitud y desapareció de todos lados").** La única acción sobre una solicitud era "Marcar atendido" — ponía `atendido = true` y ahí terminaba, sin crear ni vincular nada a ningún comercio real. Se reemplazó por dos acciones explícitas: **"Aceptar y crear comercio"** (lleva nombre/ciudad/contacto/email de la solicitud al formulario de alta de comercio que ya existía arriba, para que Jhey revise y complete lo que falte — como los datos de pago, que la solicitud nunca captura — antes de crear el comercio de verdad; la solicitud recién queda `atendido = true` cuando ese comercio se crea con éxito, nunca antes) y **"Descartar"** (equivalente a lo que hacía "Marcar atendido" antes, para solicitudes que de verdad no van a ningún lado). Con esto, aceptar una solicitud ya no es un callejón sin salida — siempre termina en un comercio real con su correo listo para que se autoactive, o queda explícitamente descartada.

**Nota de alcance:** no se creó ninguna función `SECURITY DEFINER` nueva para "aceptar y crear comercio" — se reutilizó el mismo formulario e inserción directa que el admin ya usaba para dar de alta comercios a mano (protegido por la misma policy `admins_insert_comercios`, ya probada), evitando duplicar lógica.

**4. Bandera "solo canjea" por comercio (reemplaza la idea original de Jhey de "dos campos marcables" por uno solo, más simple y sin ambigüedad).** Un comercio marcado `solo_canje = true` no puede asignar puntos (solo recibirlos cuando un cliente canjea) — el resto de los comercios (como "talleres" hoy) siguen pudiendo hacer las dos cosas, exactamente el comportamiento actual, sin ningún cambio para ellos. La bandera se aplica **dentro de** `comercio_portal_otorgar_puntos` (rechaza con un mensaje claro si el comercio es solo-canje), nunca solo ocultando el botón del lado del navegador — mismo criterio de seguridad de todo el proyecto. En `admin/index.html`, columna nueva "Solo canjea" con una casilla por comercio (actualiza directo, protegida por la misma RLS `admins_update_comercios` ya existente — no hace falta una función nueva para cambiar un flag de configuración). En `comercio/index.html`, si el comercio logueado tiene `solo_canje = true`, la sección "Asignar puntos a un cliente" se oculta y se reemplaza por un aviso explicando por qué ("Redimir puntos" sigue disponible igual).

**Verificado con datos y usuarios de prueba reales contra Supabase (creados y limpiados después, sin tocar "talleres" ni jheison68):** reclamo de una fila anónima preexistente por su dueño real conserva `id`/saldo y llena nombre/ciudad/celular/correo; un segundo usuario intentando reclamar el mismo documento ya vinculado es rechazado con el mensaje de anti-secuestro exacto; `cliente_portal_estado()` sin ningún parámetro devuelve los datos correctos de la sesión autenticada real, y sin sesión (`anon`) rechaza con "No hay ningún cliente vinculado a esta sesión"; la firma vieja de 2 parámetros ya no existe (`function does not exist`) — confirma que el hueco de seguridad quedó cerrado a nivel estructural, no solo lógico. Activación de comercio: correo pendiente se activa y vincula bien (probado con mayúsculas/espacios extra, normaliza igual); un segundo intento de activar el mismo correo ya activado falla limpio; comercio marcado `solo_canje = true` rechaza `comercio_portal_otorgar_puntos` con el mensaje correcto antes de llegar siquiera a buscar al cliente. `get_advisors(security)` corrido después de todas las migraciones y re-verificado tras el fix del default privileges: sin `anon` en ninguna de las 7 funciones que debían quedar restringidas.

**Verificado también con navegador real (Playwright, red interceptada) en los tres paneles, cero errores de consola en todos los casos:** en `cliente/index.html` — crear cuenta completa entra directo a la app con el saldo real, cerrar sesión y volver a entrar con "Iniciar sesión" funciona igual, "Olvidé mi contraseña" muestra el mensaje de confirmación, e intentar crear cuenta con un documento ya registrado se rechaza sin llegar a crear nada; en `comercio/index.html` — activación con correo no pendiente muestra el mensaje genérico correcto, con correo pendiente pide contraseña y entra a la app, y un comercio con `solo_canje = true` no ve la sección de asignar puntos (solo el aviso); en `admin/index.html` — "Aceptar y crear comercio" prellena el formulario de alta con los datos de la solicitud, al crear el comercio la solicitud desaparece de "Solicitudes" y el comercio aparece en la tabla con badge "Pendiente de activar", y la casilla "Solo canjea" se puede marcar/desmarcar por comercio.

**Pendiente, acción manual de Jhey (informado una sola vez, como corresponde):** los correos de recuperación de contraseña (cliente y comercio) y de confirmación de cuenta dependen de la configuración de correo del proyecto en el Dashboard de Supabase (Authentication → URL Configuration: Site URL y Redirect URLs: hay que agregar las URLs reales de `/cliente/` y `/comercio/` para que el enlace del correo vuelva a la página correcta; y opcionalmente Authentication → Email Templates/SMTP si se quiere personalizar el remitente). Sin este paso, `resetPasswordForEmail`/`signUp` van a funcionar del lado de la base de datos pero el correo puede no llegar o el enlace puede no volver a la página esperada — es 100% configuración de Supabase, no hay ninguna herramienta disponible en esta sesión para hacerlo por Jhey.

## Fase 8 — CRM de cumplimiento AML/SAGRILAFT-SARLAFT para comercios — hecho y verificado, 28 ago

Pedido de Jhey: ocultar "Comercios solo-canje" (sin borrar código) y convertir "Solicitudes de comercios" en un CRM real de revisión — con datos del representante legal, documentos, listas restrictivas y auditoría continua — para prevenir que un negocio use la plataforma para lavar dinero o activos.

**Bug real reportado en vivo por Jhey mientras probaba, corregido antes de seguir con el CRM:** `"permission denied for function _normalizar_para_codigo"` al crear un comercio desde el panel. Causa: el trigger `_trigger_generar_llave_comercio` (dispara al insertar en `comercios_solo_canje`) no era `SECURITY DEFINER`, así que corría con los privilegios de `authenticated` — y `_normalizar_para_codigo` tenía `EXECUTE` revocado de `authenticated` desde una sesión anterior (fix que en su momento solo cubrió el trío del lado cliente, nunca el equivalente del lado comercio). Bug latente desde el 27, nunca antes ejercido porque ningún comercio se había creado por el panel con un rol no-`postgres` hasta la prueba de Jhey. Corregido marcando `_trigger_generar_llave_comercio()` (y, preventivamente, `_trigger_generar_llave_cliente()`, mismo patrón latente) como `SECURITY DEFINER` — verificado creando y borrando un comercio de prueba real end-to-end.

**Diseño: ningún comercio se crea de forma directa nunca más.** Se eliminó la policy `admins_insert_comercios` (el único camino que permitía `insert` directo en `comercios_solo_canje`). El único camino para que nazca un comercio real es `admin_compliance_aprobar()` sobre un expediente de cumplimiento ya revisado.

**Esquema nuevo:** `comercio_compliance` (expediente por comercio — datos del negocio, datos completos del representante legal/persona encargada, origen de fondos declarado, estado `pendiente_revision → en_revision → aprobado/rechazado`, más `suspendido` para uno ya activo, nivel de riesgo, checklist de listas restrictivas en jsonb, reincidencia calculada automáticamente, `proxima_revision_at` para la auditoría periódica), `comercio_compliance_documentos` (cédula representante / RUT / cámara de comercio / otro, contra un bucket de Storage privado nuevo `comercio-documentos` — primer uso de Storage en este proyecto), y `comercio_compliance_auditoria` (bitácora append-only de cada cambio de estado, nunca se escribe directo). RLS: `is_admin()` únicamente en las tres.

**Funciones nuevas, todas `SECURITY DEFINER` con guardas de transición de estado explícitas:** `admin_compliance_crear`, `admin_compliance_actualizar`, `admin_compliance_registrar_documento`, `admin_compliance_aprobar` (crea el comercio real si no existe todavía), `admin_compliance_rechazar` (motivo obligatorio), `admin_compliance_suspender` (motivo obligatorio), `admin_compliance_reactivar`, `admin_compliance_registrar_auditoria` (marca la re-revisión periódica hecha, empuja `proxima_revision_at` otros 6 meses). También `_compliance_es_reincidente(documento)`: reincidencia = mismo documento del representante ya apareció en otro expediente `rechazado`/`suspendido` — se recalcula solo en cada creación/edición.

**El candado en operación, no solo en creación (pedido explícito de Jhey: "debe ser ambas"):** `mi_comercio_id()` — la función que usan prácticamente todas las funciones self-service del portal de comercio (`comercio_portal_otorgar_puntos`, `redimir_puntos`, `crear_campana`, `historial`, etc.) para identificar de qué comercio es la sesión — ahora devuelve `null` si ese comercio tiene un expediente en estado `suspendido`. Un solo punto de cambio bloquea toda la operación del comercio, sin tocar cada función una por una.

**Bug de permisos encontrado y corregido durante la propia verificación de este CRM (mismo patrón que el reportado por Jhey, pero nuevo):** las funciones `admin_compliance_*` recién creadas quedaron con `EXECUTE` otorgado a `PUBLIC` (el comportamiento por defecto de Postgres al crear una función nueva) — a diferencia del hallazgo anterior (que era un grant explícito a `anon` vía `pg_default_acl` de este proyecto), acá el problema fue el privilegio por defecto de Postgres sobre `PUBLIC`, que `revoke ... from anon` no toca. Corregido con `revoke execute ... from public` explícito en las 10 funciones nuevas — re-verificado que `anon` ya recibe `permission denied` real (no la excepción interna de `is_admin()`).

**Verificado con datos y usuarios de prueba reales contra Supabase (creados y borrados después):** ciclo completo crear expediente → aprobar (crea el comercio real) → vincular usuario → confirmar que opera → suspender → confirmar que `mi_comercio_id()` y toda función que depende de ella quedan bloqueadas → reactivar → confirmar que vuelve a operar. Rechazo de un expediente probado, y que ya no se puede "aprobar" algo rechazado. Reincidencia probada creando un segundo expediente con el mismo documento de representante que uno ya rechazado — se marca sola. No-admin no puede crear expedientes (`No autorizado`); `anon` no puede ni ejecutar la función (`permission denied`).

**Verificado también con navegador real (Playwright, red interceptada), cero errores de consola:** "Comercios solo-canje" queda oculto (sección y enlace de navegación) sin borrar su HTML/CSS/JS; el nuevo CRM en "Solicitudes de comercios" carga solicitudes nuevas y expedientes por pestaña de estado; abrir un expediente desde una solicitud prellena sus datos; el formulario de expediente completo (negocio, representante legal, checklist de 6 listas restrictivas, documentos, motivo) renderiza sin errores; un expediente `en_revision` muestra los botones Aprobar/Rechazar; un expediente `aprobado` muestra en cambio el panel operativo del comercio (llave, estado del portal, recarga de billetera, toggle "solo canjea", botón de auditoría periódica) y el botón Suspender.

**Entregable nuevo:** guía de referencia práctica de AML/SARLAFT (qué listas consultar con enlace directo a cada una, qué genera alerta, cómo clasificar el riesgo, cómo se traduce todo en los estados del CRM) — publicada como página propia para consulta diaria, explícitamente aclarada como marco técnico de partida y no asesoría legal formal.

**Decisión que no le correspondía tomar a esta sesión, señalada explícitamente a Jhey:** qué proveedor de listas de sanciones/PEP contratar (si en algún momento se quiere automatizar esa consulta en vez de hacerla manual) es una decisión de negocio suya — no se eligió ningún proveedor. Se entregó en cambio el checklist manual + la guía de referencia, y la única automatización interna que sí se construyó (reincidencia por documento) no depende de ningún proveedor externo.

**Pendiente / no incluido en esta sub-tarea (fuera de alcance, anotado para no repetir la pregunta):** no se conectó ningún proveedor externo de screening de listas PEP/sanciones (queda como consulta manual vía los enlaces de la guía); no se automatizó el reporte a la UIAF ni se redactó un manual formal de SARLAFT — eso depende de la consulta jurídica fintech ya bloqueada (ver tabla de bloqueos). El expediente de compliance vive separado de `comercios_solo_canje` a propósito (un comercio puede tener más de una revisión histórica sin perder el rastro).

**Ajustes sobre el CRM de cumplimiento, pedidos por Jhey después de probarlo en vivo con un comercio real ("odonto aleja") — hecho y verificado, 28 ago:**

- **Datos de pago estructurados.** Se reemplazó el campo libre "Datos de pago" del expediente por tres campos (`banco`, `tipo_cuenta` ahorros/corriente, `numero_cuenta`) más un nuevo tipo de documento adjunto `certificado_bancario`. Al aprobar, esos tres campos se combinan en el texto que recibe `comercios_solo_canje.datos_pago` (ej. "Bancolombia - Ahorros - 9988776655"). El único expediente real que ya existía ("odonto aleja", dato viejo `24562353522`) se migró preservando ese valor en `numero_cuenta` en vez de perderlo.
- **Cargo del representante como lista desplegable:** Representante legal / Propietario/a / Gerente / Administrador/a / Socio/a / Apoderado/a / Otro. Si un expediente ya tenía un valor libre que no está en la lista (como "dueña" en el expediente real existente), se preserva como opción extra en vez de perderse al guardar.
- **"+ Nuevo expediente" ahora se puede cerrar sin diligenciar nada** — reclickeando el mismo botón, o con un botón "Cancelar" explícito dentro del formulario.
- **Listas restrictivas con enlace directo:** cada ítem del checklist (OFAC, ONU, UE, Procuraduría, Contraloría, Policía Nacional) tiene ahora un botón "Consultar ↗" que abre la página oficial en una pestaña nueva, mismos enlaces que ya estaban en la guía de referencia.
- **Cadencia de re-revisión bajada de 6 a 4 meses**, contados desde la fecha de aprobación (o desde la última auditoría periódica registrada). Se recalculó también el expediente real ya aprobado para que quede en la nueva cadencia.
- **Nueva pestaña "Revisión periódica"** en el CRM: agrupa los comercios `aprobado` cuya próxima revisión ya venció (no cambia su estado ni los bloquea — siguen operando normal). Al abrir un comercio aprobado, se calculan y muestran solas las métricas del periodo transcurrido desde la última revisión: puntos otorgados, clientes distintos, canjes realizados, y una lista de clientes con actividad repetida (≥3 veces ganando o canjeando en ese mismo comercio en el periodo) como señal a revisar con criterio propio, no como bloqueo automático. Función nueva `admin_compliance_metricas_periodo`, `SECURITY DEFINER`, admin-only.
- **Panel operativo del comercio ampliado con "Campañas activas"** (mismo tag que tenía la tabla vieja de "Comercios solo-canje"), para tener billetera + campañas + toggle "solo canjea" a la vista sin tener que ir a otro lado.

**Bug de permisos real, encontrado otra vez al recrear funciones con nueva firma (mismo patrón, tercera vez este proyecto):** `admin_compliance_crear` y `admin_compliance_actualizar` tuvieron que recrearse (`DROP` + `CREATE`) porque cambiaron sus parámetros (quitar `p_datos_pago`, agregar `p_banco`/`p_tipo_cuenta`/`p_numero_cuenta`) — recrear una función resetea sus permisos a los de fábrica del proyecto, así que `anon` volvió a tener `EXECUTE` en ambas (y en la función nueva `admin_compliance_metricas_periodo`) hasta que se revocó explícitamente. Las funciones que solo cambiaron de cuerpo sin cambiar de firma (`admin_compliance_aprobar`, `reactivar`, `registrar_auditoria`) se actualizaron con `CREATE OR REPLACE` y conservaron sus permisos sin tocar nada — la lección que ya quedaba anotada de sesiones anteriores se confirma: cualquier `DROP FUNCTION` + `CREATE` en este proyecto exige re-revisar y re-revocar `EXECUTE` de `anon`/`PUBLIC`, no importa cuántas veces ya se haya hecho antes.

**Bug real reportado por Jhey en vivo, diagnosticado con evidencia de logs, NO es un bug de este código:** al activar la cuenta de "odonto aleja" (`aleja@gmail.com`) desde `comercio/index.html`, `auth.signUp()` devuelve `400 - Email address "aleja@gmail.com" is invalid` (`error_code: email_address_invalid`). Se confirmó con los logs reales de Auth del proyecto (`auth_logs`) que el correo llega limpio (sin espacios ni caracteres raros, 15 caracteres exactos) y que el código de `comercio/index.html` ya lo manda con `.trim()` — el rechazo pasa dentro de GoTrue (el servicio de Auth de Supabase), no en este repo. La causa más probable, según la documentación pública de Supabase: el proyecto tiene configurada una restricción de dominios de correo permitidos ("Domain Whitelist"/dominios permitidos) en **Authentication → Settings** del Dashboard de Supabase, y `gmail.com` no está en esa lista — cosa que rompería el registro de cualquier comercio con correo personal, no solo este caso. **Acción manual de Jhey:** revisar esa configuración (Authentication → Settings, restricción de dominios de email) y quitarla o agregar los dominios que hagan falta — no hay ninguna herramienta disponible en esta sesión para ver ni cambiar esa configuración del Dashboard.

## Parte 6 — login por documento (cliente y comercio), banner de error visible en el perfil del cliente, y admin en subpáginas — hecho y verificado, 28 ago

Pedido de Jhey, tres cosas en un mismo mensaje, con captura de pantalla del perfil del cliente mostrando "Saldo disponible —" / "Tu llave —" sin ninguna explicación visible.

**1) Diagnóstico del perfil "feo"/roto — no era un bug de datos, era un error real sin mostrarse donde se ve.** La cuenta con la que Jhey estaba probando (su propio login admin) nunca tuvo una fila vinculada en `clientes_puntos` — confirmado con una consulta real cruzando `auth.users` contra `clientes_puntos`. `cliente_portal_estado()` sí lanza una excepción clara (`No hay ningún cliente vinculado a esta sesión`) en ese caso, pero el código solo escribía ese mensaje en `#app-error`, un `<div>` escondido dentro de la pestaña "Historial", lejos de la tarjeta de saldo — así que la tarjeta se quedaba con sus placeholders "—" sin que se viera ninguna explicación. Se agregó un banner de error visible justo debajo del saludo, arriba de la tarjeta de saldo, que se muestra automáticamente cuando esta consulta falla ("Esta cuenta no tiene un cliente de puntos vinculado, por eso no vemos saldo ni llave..."). Verificado con Playwright: al forzar el mismo error, el banner aparece con el mensaje correcto y ya no se ve como una pantalla rota sin explicación.

**2) Login por número de documento (cliente y comercio) en vez de email.** El pantalla de "Iniciar sesión" (cliente) y "Entra a tu cuenta" (comercio) ya no piden correo — piden tipo + número de documento (cliente) o número de documento del comercio. La contraseña la sigue manejando Supabase Auth internamente contra el email real, pero ese email nunca vuelve a viajar al navegador ni se le vuelve a mostrar al usuario en ningún punto del login.

- **Cómo se resuelve sin exponer el email:** función Edge nueva `login-documento` (Deno, `verify_jwt=false` porque implementa su propia autenticación documento+contraseña — la misma excepción ya documentada para `otorgar-puntos`/`solicitar-canje`). Con la service role key, busca el email interno por documento en `clientes_puntos`/`comercios_solo_canje`, y recién ahí llama `signInWithPassword` contra Supabase Auth. Si el documento no existe, igual dispara un intento de login con un email inventado (mismo tiempo de respuesta, mismo mensaje genérico "Documento o contraseña incorrectos") para no delatar por temporización si el documento existe o no. El anti-fuerza-bruta que ya existía para comercios (`comercio_login_verificar_bloqueo`/`registrar_fallo`/`registrar_exito`, indexado por email) se reutilizó tal cual desde dentro de la función, sin tocar su lógica.
- **"¿Olvidaste tu contraseña?" también pasó a pedir documento** (mismo endpoint, `accion: "recuperar"`) — sigue mandando el enlace al correo ya registrado, y responde igual exista o no el documento, para no poder usarse para averiguar qué documentos tienen cuenta.
- **Fuera de alcance a propósito, para no tocar más de lo pedido:** "Crear cuenta" (cliente) y "Activa tu cuenta" (comercio, self-service) siguen pidiendo el correo tal cual — son procesos de una sola vez donde Puntos Neggo necesita un correo real (para crear la cuenta de Auth o para verificar que corresponde al correo con el que se afilió el negocio), no el login del día a día. Si Jhey quiere que estos también dejen de pedir correo, es un cambio aparte — avisado, no implementado.
- **`comercios_solo_canje` no tenía número de documento propio** (solo vivía en el expediente de cumplimiento) — se agregó la columna `numero_documento`, se rellenó automáticamente desde `comercio_compliance.numero_documento_negocio` para el comercio real que ya existía ("odonto aleja" → `1017269398`), quedó con restricción `unique`, y `admin_compliance_aprobar()` la completa sola en cada aprobación nueva de ahí en adelante — sin backfill manual futuro.
- **Verificado con Playwright (red interceptada):** el campo de login del cliente ya es documento (ya no existe `#entrar-email`), login exitoso entra a la app, y la misma reescritura se probó en el comercio. **No se pudo probar la función Edge en vivo contra Supabase real desde esta sesión** — este entorno en la nube no tiene salida de red hacia `supabase.co` (confirmado: hasta un `curl` simple se cae) — así que la función se verificó por revisión de código cuidadosa, no con una llamada real de extremo a extremo. La prueba real de punta a punta con una cuenta real de comercio sigue bloqueada además por el bug ya documentado de `email_address_invalid` (nadie puede terminar de activar "odonto aleja" todavía) — en cuanto Jhey resuelva esa configuración del Dashboard, vale la pena que pruebe este login una vez con una cuenta real antes de darlo por completamente cerrado.

**3) Panel de admin: cada pestaña ahora es una subpágina real, no scroll.** Antes, "Resumen"/"Cuenta por comercio"/"Canjes pendientes"/"Comercios (cumplimiento)" eran anclas (`#sec-x`) que hacían scroll suave dentro de una sola página larga con las cuatro secciones apiladas. Ahora solo se ve una sección a la vez — las otras quedan con `display:none` — y el clic en cada pestaña (o cambiar el hash de la URL) alterna cuál se muestra, con scroll al inicio de la página en cada cambio. Los enlaces `#sec-pasivo`/`#sec-cuenta-comercio`/`#sec-canjes`/`#sec-solicitudes` que Jhey ya venía compartiendo siguen funcionando exactamente igual como enlaces directos. `sec-comercios` (la sección de "Comercios solo-canje" oculta desde la Parte 5) quedó deliberadamente fuera de las rutas válidas — ni siquiera escribiendo `#sec-comercios` a mano en la URL se puede volver a ver, cae a "Resumen". Verificado con Playwright: al cargar sin hash se ve solo "Resumen"; al hacer clic en "Comercios (cumplimiento)" se oculta "Resumen" y aparece esa sección con el hash actualizado; un enlace directo a `#sec-canjes` muestra esa sección de entrada. También se volvió a correr la batería de pruebas completa del CRM (Parte 5) navegando primero a la pestaña correspondiente — sigue funcionando exactamente igual que antes, cero errores de consola.

## Resumen de bloqueos activos hoy

| Bloqueo | Tipo | Quién lo resuelve |
|---|---|---|
| Plan de Supabase (¿sigue en Free con 2 proyectos activos, o ya subió a Pro?) | Externo/facturación | Jhey — confirmar estado real |
| Proveedor financiero regulado | Externo/gestión | Jhey |
| Consulta jurídica fintech | Externo/gestión | Jhey |
| Verificación de identidad real en `neggo-12` | Producto hermano, fuera de este repo | Jhey decide prioridad |
| Cómo está integrado hoy `neggo-12` ↔ `ads-ai-platform` técnicamente | Investigación técnica | Se resuelve en Fase 3, revisando código real |
| Configuración de correo de Supabase (Site URL / Redirect URLs, y opcionalmente SMTP) para que los enlaces de recuperación de contraseña y confirmación de cuenta funcionen bien en producción | Externo/Dashboard de Supabase | Jhey — Authentication → URL Configuration |
| Proveedor de screening automático de listas PEP/sanciones (si se quiere automatizar la consulta de la guía AML en vez de manual) | Decisión de negocio/proveedor | Jhey |
| Restricción de dominios de email en Authentication → Settings del Dashboard de Supabase (bloqueando gmail.com al activar "odonto aleja") | Externo/Dashboard de Supabase | Jhey — Authentication → Settings |
| Manual formal de SARLAFT y reporte a la UIAF | Depende de la consulta jurídica fintech (ya bloqueada arriba) | Jhey |
| Probar `login-documento` de punta a punta con una cuenta real de comercio (bloqueado hasta que se resuelva el `email_address_invalid` de arriba) | Depende del bloqueo de dominios de email | Jhey |

Nada de esto bloquea empezar la Fase 1 hoy mismo.

## Parte 7 — campos "feos" en el formulario del cliente y nueva sección "Clientes" en admin — hecho y verificado, 28 ago

Jhey mandó captura del formulario "Crear cuenta" del cliente: los campos "Correo", "Celular" y "Contraseña" se veían con un borde delgado blanco distinto al resto ("Nombres", "Tipo de documento", "Número de documento", "Ciudad"), que sí tenían el fondo relleno y sin borde visible.

**Causa: no era nuevo, ya estaba así antes de la Parte 6.** El CSS de `cliente/index.html` define el estilo "bonito" (fondo `--bg-soft`, sin borde visible, radio de esquina) con el selector `select, input[type="text"], input[type="number"]` — que deja afuera a `input[type="email"]`, `input[type="tel"]` e `input[type="password"]`, así que esos tres campos caían al estilo por defecto del navegador. `comercio/index.html` y `admin/index.html` no tienen este problema — ahí el selector base es genérico (`input, select` sin filtrar por tipo). Corregido agregando esos tres tipos al selector — ahora los seis campos del formulario se ven exactamente iguales. Verificado con Playwright comparando el color de fondo calculado de los seis campos: los seis quedan idénticos.

**Nueva sección "Clientes" en el panel de admin.** No existía ninguna forma de ver la ficha de un cliente desde admin — solo se veía un id truncado de 8 caracteres en la tabla de canjes. Ahora hay una pestaña "Clientes" (entre "Resumen" y "Cuenta por comercio") con una tabla buscable por nombre, documento o llave (nombre, documento, llave, teléfono, ciudad, saldo, estado de la cuenta activa/congelada, aviso si tiene la redención bloqueada) y un botón "Ver detalle" que despliega su actividad reciente (últimos 100 movimientos: fecha, tipo, puntos, comercio/contraparte, motivo) sin salir de la fila. Por ahora es de solo lectura -- no se agregó ninguna acción de editar/congelar/desbloquear desde aquí, porque no fue lo que se pidió; avisado por si se quiere sumar después.

**Cómo se resolvió el acceso a los datos sin abrir hoyos nuevos de seguridad:** el listado en sí ya era legible por admin (la tabla `clientes_puntos` ya tenía la policy RLS `admins_select_clientes_puntos` desde antes), así que la tabla se carga con una consulta directa igual que ya se hace para comercios. El detalle de movimientos es distinto: `puntos_movimientos` no tiene ninguna policy RLS (a propósito, se lee solo a través de funciones) — se agregó `admin_cliente_movimientos(p_cliente_id)`, `SECURITY DEFINER` con el mismo guard `is_admin()` que el resto de funciones admin, revocada de `anon`/`PUBLIC` igual que siempre.

**Verificado con Playwright:** los seis campos del formulario de "Crear cuenta" quedan con el mismo fondo; la sección "Clientes" carga los clientes de prueba, el buscador filtra en vivo, los badges de estado/bloqueo aparecen correctos, y "Ver detalle" despliega los movimientos reales del cliente. Se corrió de nuevo toda la batería de regresión de las Partes 5 y 6 (CRM de cumplimiento + login por documento + subpáginas del admin) — sigue funcionando igual, cero errores de consola.

## Parte 7 (cont.) — correo del cliente visible en admin, y confirmación de alcance del login por documento — hecho y verificado, 28 ago

Jhey preguntó si el login por documento quedó establecido para todos (cliente y comercio), y pidió que admin también pueda ver el correo del cliente.

**Confirmación del alcance del login por documento — no hay ninguna otra pantalla de login en el repo.** Se revisaron los cinco archivos HTML del proyecto (`cliente`, `comercio`, `admin`, `aliados`, la landing raíz): `aliados/index.html` y la landing (`index.html`) no tienen ningún login — son formularios públicos de "quiero afiliar mi negocio" que alimentan `solicitudes_comercio`, no cuentas con sesión. Los únicos dos logins reales del sistema son `cliente/index.html` y `comercio/index.html`, y ambos ya piden documento (Parte 6). Se mantiene la única excepción ya avisada antes: "Crear cuenta" (cliente) y "Activa tu cuenta" (comercio, self-service) siguen pidiendo correo porque ahí se está creando o verificando la cuenta de Auth de Supabase, que técnicamente necesita un correo real — no es una pantalla de login del día a día, es un alta de una sola vez.

**Correo del cliente en la ficha de admin — no bastaba con agregar la columna.** `clientes_puntos.email` es una columna libre que el registro self-service (`cliente_portal_registrar`) nunca llena — iba a salir vacía para casi todos los clientes reales. El correo verdadero de cada cliente vive en `auth.users`, vinculado por `user_id`, y esa tabla no es legible desde el navegador (ni con RLS ni por PostgREST). Se creó `admin_listar_clientes()`, `SECURITY DEFINER` admin-only, que hace el cruce del lado del servidor: `coalesce(auth.users.email, clientes_puntos.email)` — prioriza el correo real de la cuenta de Auth y cae al campo libre solo si por algún motivo no hay `user_id` vinculado todavía (como el único registro de prueba que ya existía). Reemplaza la consulta directa que la sección "Clientes" usaba antes. Revocada de `anon`/`PUBLIC` igual que toda función nueva de este proyecto — verificado con `information_schema.routine_privileges` que solo quedan `authenticated`/`service_role`/`postgres`.

**Verificado:** la lógica del cruce se probó con una consulta real (el cliente de prueba sin `user_id` sí devuelve su correo por el fallback a la columna libre) y con Playwright (la tabla de "Clientes" ahora muestra la columna Correo, el buscador también filtra por correo, y el resto de la sección sigue funcionando igual). Se corrió de nuevo toda la batería de regresión — sin errores nuevos.

**Bug real reportado en vivo por Jhey al abrir "Clientes": `structure of query does not match function result type` — corregido y verificado, 28 ago.** Causa: `auth.users.email` es `character varying`, no `text`, y `admin_listar_clientes()` declaraba la columna `correo` como `text` en su `RETURNS TABLE` — el `coalesce(u.email, cp.email)` quedaba con un tipo que no calzaba exactamente con lo declarado, y `RETURN QUERY` en PL/pgSQL exige coincidencia exacta de tipo, no solo compatible. Corregido con un cast explícito (`u.email::text`) dentro del `coalesce`. Misma firma, así que `CREATE OR REPLACE` bastó — no hubo que re-revocar `anon`/`PUBLIC` (confirmado con `information_schema.routine_privileges`, sin cambios). **Esta vez sí se verificó ejecutando la función real como el admin** (impersonando su sesión con `request.jwt.claims` en una consulta directa, no solo revisando el código) — devolvió correctamente la fila de Jheison con su correo real. No hizo falta ningún cambio en el frontend; el bug era 100% del lado de la función SQL.

## Parte 8 — bug del dashboard de comercios, notificación de revisión periódica pendiente, y conteo de revisiones en el expediente — hecho y verificado, 29 ago

Jhey reportó `JSON object requested, multiple (or no) rows returned` en el dashboard de comercios, y pidió tres cosas más (items 3 y 4 de su mensaje; los items 1, 2 y 5 se tratan aparte porque son de otra naturaleza — datos, configuración externa y una funcionalidad nueva).

**Bug real: `cargarInfoComercio()` en `comercio/index.html` no filtraba por `user_id` — causa raíz confirmada, no supuesta.** La tabla `comercios_solo_canje` tiene dos políticas RLS permisivas de `SELECT` (`admins_select_comercios` con `is_admin()`, y `comercio_select_propio` con `user_id = auth.uid()`) — Postgres combina políticas permisivas del mismo comando con `OR`, así que una cuenta admin ve TODAS las filas de la tabla, no solo la suya. La consulta original de `cargarInfoComercio()` no tenía ningún `.eq()`, solo `.maybeSingle()` — funcionaba bien para un comercio normal (1 fila propia) pero reventaba en cuanto la cuenta que abre el portal es también admin y además tiene un comercio de prueba vinculado (el caso real de Jhey, por el comercio de prueba "talleres" enlazado a su propio `user_id` de admin) — con más de una fila visible, `.maybeSingle()` tira exactamente ese error. Corregido agregando `.eq("user_id", session?.user?.id)` explícito antes del `.maybeSingle()`, para que la consulta dependa del usuario de la sesión y no de lo que RLS le deja ver como admin.

**Verificado con evidencia real, no solo revisando el código:** reproduje el bug impersonando la sesión de un admin con comercio de prueba vinculado en una consulta SQL directa (`request.jwt.claims` + `set local role authenticated`) — la consulta sin filtro devolvía 2 filas (el bug), la misma consulta con el filtro por `user_id` devolvía exactamente 1. Después corrí Playwright contra `comercio/index.html` con una sesión simulada y confirmé que ya no aparece ningún error y que el comercio correcto se muestra.

**Item 3 — notificación numérica en "Revisión periódica" quedó lista.** Se agregó una función `actualizarBadgeRevisionPeriodica()` que cuenta (`count: "exact", head: true`, sin traer filas) los expedientes con `estado = 'aprobado'` y `proxima_revision_at` ya vencida, y pinta el número en un badge tanto en el link principal del menú ("Comercios (cumplimiento)") como en la pestaña "Revisión periódica". Se llama al cargar el panel y de nuevo cada vez que se registra una auditoría periódica (para que el número baje al instante sin recargar la página). Si no hay pendientes, el badge se oculta en vez de mostrar "0".

**Item 4 — conteo de revisiones periódicas realizadas, visible en el expediente.** En el panel operativo de cada comercio (`cargarPanelOperativo`) se agregó una tercera consulta en paralelo contra `comercio_compliance_auditoria` (`count: "exact", head: true`, filtrando `compliance_id` + `evento = 'revision_periodica'`) y una nueva casilla "Revisiones periódicas realizadas" en la grilla, entre "Próxima re-revisión" y "Campañas activas".

**Verificado con Playwright:** con datos simulados (3 comercios pendientes de revisión, y un expediente con 5 revisiones ya registradas), el badge muestra "3" en ambos lugares y queda visible (no oculto); el conteo de revisiones se leyó correctamente desde la consulta agregada. Se corrió de nuevo la batería completa de regresión (Parte 5, 6, 7) — sin errores nuevos.

**Item 1 — limpieza de datos de prueba, ejecutada y verificada, 29 ago.** Se inventarió primero todo lo relacionado antes de borrar nada: la tabla `comercios_solo_canje` solo tenía 2 filas — "talleres" (de prueba, y vinculada al `user_id` de la propia cuenta admin de Jhey, la causa exacta del bug de arriba) y "odonto aleja" (real, doc. 1017269398 — la que había que conservar). Se confirmó con `pg_constraint` que `puntos_movimientos.cliente_id` y `canjes.cliente_id` sí tienen foreign key hacia `clientes_puntos` (a diferencia del vínculo con el comercio, que no está forzado), y se verificó con una consulta real que las 13 filas de `puntos_movimientos` y las 10 de `canjes` ligadas a "talleres" pertenecen al 100% a un único cliente de prueba (Jheison Arley Castaño Flórez, sin `user_id` — es decir, nunca fue una cuenta real de Auth, solo una fila insertada a mano) y que ese cliente no tiene ningún movimiento fuera de esos mismos 27. Con eso confirmado (nada real quedaba huérfano), se borró en orden (`canjes` → `puntos_movimientos` → `comercio_campanas`/`comercio_recargas` → `clientes_puntos` → `comercios_solo_canje` → la solicitud duplicada "ortodoncia alejandra") dentro de una sola transacción. **Verificado después con conteos reales:** `clientes_puntos` en 0, `comercios_solo_canje` con una sola fila ("odonto aleja", doc. 1017269398), `solicitudes_comercio` con una sola fila, y `comercio_compliance` intacta (1 fila, la de "odonto aleja") — nada tocó al comercio real que Jhey pidió conservar.

**Item 2 — diagnóstico de por qué "recuperar contraseña" no funciona (cliente).** Revisando el flujo completo (frontend en `cliente/index.html`, el edge function `login-documento`, y los datos reales en Supabase) se encontró la causa raíz real, no una suposición: la función `resolverEmail()` del edge function solo puede mandar el enlace de recuperación si encuentra una fila en `clientes_puntos` con `user_id` no nulo (es decir, una cuenta de Auth real, creada por "Crear cuenta"). Hoy, `auth.users` solo tiene una fila — la cuenta admin del propio Jhey — ningún cliente ha completado nunca el registro real de "Crear cuenta". El cliente de prueba que existía (el mismo que se borró en el item 1) no tenía `user_id`, así que cada vez que Jhey probaba "¿Olvidaste tu contraseña?" con ese documento, el edge function no encontraba ningún correo al que mandarle nada — y como la respuesta es deliberadamente genérica (para no revelar si un documento existe o no), la pantalla igual mostraba el mensaje de "enlace enviado" aunque no se mandó nada. Ese es el 100% de la explicación de "no funciona": no hay ningún bug que corregir en el código, el problema es que nunca se probó con una cuenta real. Paso a paso enviado a Jhey en el chat (crear una cuenta real primero, confirmar Site URL/Redirect URLs en el Dashboard de Supabase incluyendo `localhost:8000`, y considerar SMTP propio por los límites bajos del correo incluido de Supabase). No se tocó ningún archivo ni función para esto — es 100% configuración/datos de prueba, no código.

**Item 5 — checklist de vinculación AML/SARLAFT en el expediente, construido y verificado, 29 ago.** Se agregó un checklist que se recalcula en vivo (con cada tecla, cada checkbox, cada documento cargado) dentro del formulario de expediente, con tres categorías: (1) Datos del negocio y representante — 13 campos, incluyendo datos de pago y contacto; (2) Documentos — cédula del representante, RUT, certificado bancario siempre, y Cámara de Comercio solo si el tipo de negocio es "persona jurídica" (no aplica a persona natural); (3) Debida diligencia — las 6 listas restrictivas y el nivel de riesgo evaluado. Se implementó con `calcularChecklistExpediente()` (función pura, recibe el estado actual del formulario + los tipos de documento ya cargados) y `checklistExpedienteHtml()` (renderiza el resumen "X/Y diligenciado" y cada ítem en verde/pendiente), recalculado con listeners `input`/`change` sobre todo el panel del formulario (cubre campos de texto, selects, checkboxes de listas restrictivas) y de nuevo cada vez que `cargarDocumentos()` resuelve o se sube un documento nuevo. Cuando el checklist llega a 100% se muestra un banner "✓ Expediente completo — listo para revisar y aprobar cuando quieras."

**Desviación deliberada del pedido literal, avisada a Jhey.** Jhey pidió que "cuando se termine, pase ya a la sesión de aprobados como está ahora" — es decir, que se autoapruebe al completarse el checklist. No se implementó así a propósito: el checklist mide que la información está *diligenciada*, no que fue *revisada y aceptada* por un humano — son cosas distintas en cumplimiento AML/SARLAFT, y automatizar la aprobación rompería el principio que ya rige todo este sistema (reincidencia, revisión periódica, etc. son siempre señales, nunca un gatillo automático). El botón "✓ Aprobar y crear comercio" sigue siendo exactamente el mismo de antes — un click manual — el checklist solo hace más visible cuándo ya no falta nada para poder decidir.

**Verificado con Playwright:** con un expediente parcialmente diligenciado se confirmó que el resumen arranca en 7/19 y el banner de "listo para aprobar" está oculto; al completar todos los campos de datos, listas y riesgo sube a 15/19 (documentos aún pendientes) con el banner seguro oculto; al simular los 4 documentos cargados sube a 19/19 y el banner aparece — y el botón de aprobar sigue existiendo sin haberse disparado ningún RPC de aprobación automáticamente. Se corrió de nuevo toda la batería de regresión (Parte 5, 6, 7, y los nuevos tests de Parte 8) — sin errores nuevos.

Archivos entregados: `comercio/index.html` y `admin/index.html`.

## Parte 9 — guía AML/SARLAFT en el repo, checklist ampliado (PEP/riesgo), y evidencia auditable de aceptación legal — hecho y verificado, 29 ago

Jhey pidió cuatro cosas: (1) que la guía AML/SARLAFT quedara como documento real en el repo (no solo como lógica dentro del código) y recordó hacer commit/push de todo; (2) la lista de documentos que un asesor debe pedirle a un comercio, en formato guía, con el formulario ajustado si hace falta; (3) quedar listos para empezar a vincular comercios; (4) que la aceptación de términos y tratamiento de datos quede como evidencia auditable, visible (poco vistosa) en el portal de cliente y de comercio.

**Item 1 y 2 — guía nueva en `docs/guia-aml-sarlaft-documentos-comercio.md`.** Documento de uso interno para el asesor: cómo clasificar un comercio en 3 preguntas (persona natural/jurídica, actividad de riesgo estándar o alto, PEP o no), qué documentos pedir en cada caso, y un guion de lenguaje simple para explicárselo a un dueño de negocio que no conoce términos legales. Aclara honestamente que Puntos Neggo no es una entidad vigilada con SARLAFT formal — es debida diligencia voluntaria, y recomienda revisión de un abogado/oficial de cumplimiento antes de escalar a mayor volumen.

**El formulario del expediente se amplió para que la guía se pueda aplicar de verdad, no solo en el papel:** se agregó un checkbox "El representante es una Persona Expuesta Políticamente (PEP)" (`representante_pep`, nueva columna en `comercio_compliance`) junto al selector de riesgo, y 2 tipos de documento nuevos (`declaracion_origen_fondos_ampliada`, `declaracion_pep`) al catálogo de documentos que se pueden subir — se amplió también el `CHECK` de `comercio_compliance_documentos.tipo` para permitirlos. El checklist de vinculación (Parte 8) ahora exige estos 2 documentos adicionales automáticamente solo cuando el riesgo quedó en "alto" o el checkbox de PEP está marcado — exactamente la lógica que describe la guía. `admin_compliance_actualizar()` se actualizó (mismo patrón `coalesce`, no rompe llamadas viejas) para guardar `representante_pep`.

**Item 3 — estado real de "listo para vincular comercios":** con el bug del dashboard corregido, los datos de prueba limpios, el checklist de vinculación funcionando, y la aceptación legal con evidencia (ver abajo), el sistema queda operativamente listo para procesar comercios reales. Lo único pendiente de este bloque es lo que ya se avisó antes y sigue en pie: el punto ciego de verificación de identidad externa (el documento del cliente/representante no se valida hoy contra ninguna fuente externa como Didit), y que el contenido legal de `legal/index.html` (ver abajo) es una v1 razonable, no un documento revisado por abogado.

**Item 4 — evidencia auditable de aceptación legal, con IP y user-agent reales, no solo el checkbox visual.** Antes de esto, el checkbox de "Acepto términos y tratamiento de datos" en "Crear cuenta" (cliente) ya existía y ya bloqueaba el envío del formulario si no estaba marcado — pero no dejaba ningún rastro en la base de datos; y el formulario de "Activa tu cuenta" del comercio ni siquiera tenía el checkbox. Se creó la tabla `aceptaciones_legales` (RLS: cada quien ve solo la suya, admin ve todas; sin policy de insert/update/delete para `authenticated` — solo se escribe desde función SECURITY DEFINER) y la función interna `registrar_aceptacion_legal()` (revocada de `anon`/`authenticated`/`PUBLIC`, nunca invocable directo desde el navegador), que registra 2 filas (términos + tratamiento de datos) con la versión exacta aceptada, fecha, IP y user-agent — estos dos últimos capturados del lado del servidor leyendo los headers reales de la petición (`request.headers` de PostgREST), no de algo que el navegador pueda falsificar.

`cliente_portal_registrar()` y `comercio_portal_activar()` ahora exigen `p_acepta_terminos = true` (si no, la función rechaza la creación/activación de la cuenta con un mensaje claro) y registran la aceptación en el mismo momento, atómicamente con la creación de la cuenta — no hay forma de tener una cuenta sin la aceptación registrada. Se agregó el checkbox faltante en "Activa tu cuenta" (comercio), con enlaces a `/legal/#terminos` y `/legal/#privacidad`, y ambos portales ahora muestran, en letra pequeña y en un lugar poco protagónico (dentro de la app ya logueada, no en la pantalla de login), la versión y fecha exacta que se aceptó — visible para el usuario y disponible para cualquier auditoría.

**`legal/index.html` dejó de decir "en preparación":** se redactó una versión 1 real (`v1-2026-08-29`) del reglamento, los términos de uso, y la política de tratamiento de datos — esta última ya cubre explícitamente los datos de debida diligencia AML/SARLAFT que se recolectan de los comercios. Es la versión que queda referenciada por el checkbox de aceptación. Se corrigió también la nota vieja que decía que el cliente se identificaba "sin contraseña" — ya no es cierto desde la Parte 6 (login por documento + contraseña).

**Verificado con evidencia real:** se probó `cliente_portal_registrar` y `comercio_portal_activar` impersonando la sesión de un usuario real contra la base — sin `p_acepta_terminos=true` ambas rechazan con el mensaje esperado; con `true`, ambas crean la cuenta Y dejan las 2 filas de `aceptaciones_legales` con IP/user-agent capturados correctamente de headers simulados. Se probó `admin_compliance_actualizar` con `p_representante_pep=true` y se confirmó que la columna queda en `true`. Todos los datos de prueba de esta verificación se borraron después, dejando la base exactamente como estaba (solo "odonto aleja" real). Con Playwright se confirmó: el checkbox nuevo existe y es obligatorio en ambos formularios, el RPC no se llama sin marcarlo, sí se llama con `p_acepta_terminos: true` al marcarlo, y la página de legal ya no dice "En preparación" y menciona la v1 y AML/SARLAFT. Se corrió de nuevo toda la batería de regresión (Parte 5 a 8) — sin errores nuevos.

**Sobre el commit y push a git:** el bridge al computador de Jhey perdió la conexión a la máquina virtual local varias veces durante esta sesión (el mecanismo que corre comandos de shell en su computador, no el que copia archivos) — así que no se pudo ejecutar `git add`/`commit`/`push` desde acá en este momento. Los archivos sí quedaron escritos en la carpeta del repo (eso usa un mecanismo distinto que sí funcionó). Se le dejaron a Jhey los comandos exactos para que él haga el commit y push mientras se resuelve la conexión, o para reintentarlo apenas la sesión se reconecte.

Archivos entregados: `cliente/index.html`, `comercio/index.html`, `admin/index.html`, `legal/index.html`, `docs/guia-aml-sarlaft-documentos-comercio.md` (nuevo).
